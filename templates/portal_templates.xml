<?xml version="1.0" encoding="utf-8"?>
<odoo>

<!-- ============================================================================
     Home hero — children strip + active/upcoming camps + nav row
     Replaces the 3 separate docs_entry cards with one structured block.
     ============================================================================ -->

<!-- Rename "My account" → "Panel Klienta" / "Кабінет клієнта" per legal
     documents (regulamin-panelu-klienta.html). Match by structure, not
     text — the 'My account' string is already translated at render
     time in some locales, so text() XPath would fail. -->
<template id="portal_layout_title_campscout" inherit_id="portal.portal_layout" priority="25">
    <xpath expr="//div[hasclass('wrapper')]/h3[hasclass('my-3')]" position="replace">
        <h3 class="my-3">Кабінет клієнта</h3>
    </xpath>
</template>

<template id="portal_my_home_campscout_hero" inherit_id="portal.portal_my_home" customize_show="True" name="Show CampScout Hero (children + camps)" priority="25">
    <!-- For portal-only parents: tag the root container so CSS hides the
         standard category rows (Orders/Invoices/Security). We don't touch
         .o_portal_docs or the spinner — Odoo JS keeps working. -->
    <xpath expr="//div[hasclass('o_portal_my_home')]" position="attributes">
        <attribute name="t-att-data-cs-parent-only">1 if cs_parent_only else 0</attribute>
    </xpath>
    <xpath expr="//div[hasclass('o_portal_docs')]" position="before">
        <t t-if="cs_has_hero">
            <div class="cs-hero mb-4">

                <!-- Active camp banner (show only if any child is currently at a camp) -->
                <t t-if="cs_active_regs">
                    <div class="alert alert-success cs-active-camp mb-3" role="alert">
                        <t t-foreach="cs_active_regs" t-as="reg">
                            <div class="d-flex align-items-center">
                                <div class="flex-grow-1">
                                    <strong>
                                        <t t-if="reg.participant_id" t-esc="reg.participant_id.display_name"/>
                                        <t t-else="">Ваша дитина</t>
                                    </strong>
                                    зараз у таборі
                                    <strong t-esc="reg.event_id.name"/>.
                                    <br/>
                                    <small class="text-muted">
                                        Повернення: <t t-esc="reg.event_id.date_end.strftime('%d.%m.%Y')"/>
                                    </small>
                                </div>
                            </div>
                        </t>
                    </div>
                </t>

                <!-- Children strip -->
                <t t-if="cs_participants">
                    <div class="cs-children-strip mb-3">
                        <h5 class="mb-2">Ваші діти у CampScout</h5>
                        <div class="row g-2">
                            <t t-foreach="cs_participants" t-as="child">
                                <div class="col-md-4 col-sm-6">
                                    <a t-attf-href="/my/participants/#{child.id}" class="text-decoration-none">
                                        <div class="card h-100 cs-child-card">
                                            <div class="card-body py-2 px-3 d-flex align-items-center gap-3">
                                                <!-- Avatar slot: real image when available, gender emoji fallback.
                                                     Source image comes from camp.day.photo tagging in Phase 5 (§2.7.6).
                                                     Until then, shows boy/girl/neutral child emoji. -->
                                                <div class="cs-child-avatar">
                                                    <t t-if="child.image_128">
                                                        <img t-attf-src="/web/image/camp.participant/#{child.id}/image_128"
                                                             t-attf-alt="#{child.display_name}"/>
                                                    </t>
                                                    <t t-else="">
                                                        <span class="cs-child-emoji" t-attf-data-gender="#{child.gender or 'x'}">
                                                            <t t-if="child.gender == 'f'">👧</t>
                                                            <t t-elif="child.gender == 'm'">👦</t>
                                                            <t t-else="">🧒</t>
                                                        </span>
                                                    </t>
                                                </div>
                                                <div class="flex-grow-1 min-w-0">
                                                    <div class="fw-semibold text-truncate" t-esc="child.display_name"/>
                                                    <small class="text-muted">
                                                        <t t-if="child.birth_date">
                                                            <t t-esc="(cs_today - child.birth_date).days // 365"/>
                                                            років
                                                        </t>
                                                    </small>
                                                </div>
                                            </div>
                                        </div>
                                    </a>
                                </div>
                            </t>
                        </div>
                    </div>
                </t>

                <!-- Upcoming camps -->
                <t t-if="cs_upcoming_regs">
                    <div class="cs-upcoming mb-3">
                        <h6 class="mb-2 text-muted">Наступні табори</h6>
                        <ul class="list-unstyled mb-0">
                            <t t-foreach="cs_upcoming_regs" t-as="reg">
                                <li class="mb-1">
                                    <span class="badge bg-light text-dark me-2">
                                        <t t-esc="reg.event_id.date_begin.strftime('%d.%m')"/>
                                    </span>
                                    <t t-esc="reg.event_id.name"/>
                                    <t t-if="reg.participant_id">
                                        — <span class="text-muted" t-esc="reg.participant_id.display_name"/>
                                    </t>
                                </li>
                            </t>
                        </ul>
                    </div>
                </t>

                <!-- Nav blocks (same card style as /my/documents grid) -->
                <div class="cs-doc-grid mt-4">
                    <div class="row g-3">
                        <div class="col-md-4">
                            <a href="/shop" class="cs-doc-card cs-doc-card-accent">
                                <span class="cs-doc-icon">🏕️</span>
                                <div class="cs-doc-body">
                                    <h5 class="cs-doc-title">Забронювати табір</h5>
                                    <p class="cs-doc-desc">Наступний сезон для дитини</p>
                                    <span class="cs-doc-cta">Переглянути табори →</span>
                                </div>
                            </a>
                        </div>
                        <div class="col-md-4">
                            <a href="/my/bookings" class="cs-doc-card">
                                <span class="cs-doc-icon">📋</span>
                                <div class="cs-doc-body">
                                    <h5 class="cs-doc-title">Мої замовлення</h5>
                                    <p class="cs-doc-desc">Активні бронювання та архів</p>
                                    <span class="cs-doc-cta">Переглянути →</span>
                                </div>
                            </a>
                        </div>
                        <div class="col-md-4">
                            <a href="/my/stories" class="cs-doc-card">
                                <span class="cs-doc-icon">📖</span>
                                <div class="cs-doc-body">
                                    <h5 class="cs-doc-title">Щоденні історії</h5>
                                    <p class="cs-doc-desc">Звіти вожатих з табору</p>
                                    <span class="cs-doc-cta">Переглянути →</span>
                                </div>
                            </a>
                        </div>
                        <div class="col-md-4">
                            <a href="/my/loyalty" class="cs-doc-card">
                                <span class="cs-doc-icon">🏆</span>
                                <div class="cs-doc-body">
                                    <h5 class="cs-doc-title">Лояльність</h5>
                                    <p class="cs-doc-desc">Рівні та знижки</p>
                                    <span class="cs-doc-cta">Переглянути →</span>
                                </div>
                            </a>
                        </div>
                        <div class="col-md-4">
                            <a href="/my/documents" class="cs-doc-card">
                                <span class="cs-doc-icon">📄</span>
                                <div class="cs-doc-body">
                                    <h5 class="cs-doc-title">Документи</h5>
                                    <p class="cs-doc-desc">Умови, політики, згоди</p>
                                    <span class="cs-doc-cta">Переглянути →</span>
                                </div>
                            </a>
                        </div>
                    </div>
                </div>

            </div>
        </t>
    </xpath>
</template>

<!-- ============================================================================
     Sidebar Navigation — add links to portal menu
     ============================================================================ -->

<!-- Sidebar inherit disabled: Odoo 17 portal.portal_layout has no .o_portal_aside wrapper.
     Nav is provided via home-page cards and breadcrumbs instead. Will restore once
     /my/participants/<id> detail page (per TZ §2.7.2) is built with its own layout. -->

<!-- ============================================================================
     Breadcrumbs
     ============================================================================ -->

<template id="portal_breadcrumbs_campscout" inherit_id="portal.portal_breadcrumbs" priority="20">
    <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
        <li t-if="page_name == 'stories'" class="breadcrumb-item active">
            Щоденні історії
        </li>
        <li t-if="page_name == 'documents'" class="breadcrumb-item active">
            Юридичні документи
        </li>
        <li t-if="page_name == 'loyalty'" class="breadcrumb-item active">
            Програма лояльності
        </li>
    </xpath>
</template>

<!-- ============================================================================
     /my/stories — Published camp stories
     ============================================================================ -->

<template id="portal_stories" name="Camp Stories">
    <t t-call="portal.portal_layout">
        <t t-set="additional_title">Щоденні історії з табору</t>

        <div class="cs-page">
            <div class="cs-page-back mb-3">
                <a href="/my" class="cs-back-link">← До кабінету</a>
            </div>

            <div class="cs-page-header mb-4">
                <h2 class="cs-page-title">Щоденні історії з табору</h2>
                <p class="cs-page-subtitle">Звіти вожатих про життя дітей у таборі</p>
            </div>

            <div t-if="not stories" class="cs-empty-state">
                <div class="cs-empty-icon">📖</div>
                <h4>Поки що тихо</h4>
                <p class="text-muted">
                    Історії з'являться тут одразу як вожаті опублікують перший щоденний звіт.
                </p>
            </div>

            <div t-foreach="stories" t-as="story" class="cs-story-card mb-3">
                <div class="cs-story-head">
                    <div class="cs-story-meta">
                        <time class="cs-story-date"><t t-esc="story.date.strftime('%d.%m.%Y')"/></time>
                        <span class="cs-story-type" t-attf-data-type="#{story.story_type}">
                            <t t-esc="dict([
                                ('daily_log', 'Щоденник'),
                                ('participant_spotlight', 'Портрет'),
                                ('adventure', 'Пригода'),
                                ('lesson_learned', 'Урок'),
                                ('team_moment', 'Команда'),
                                ('life_skill', 'Навичка'),
                                ('memory', 'Спомин')
                            ]).get(story.story_type, story.story_type)"/>
                        </span>
                    </div>
                    <h3 class="cs-story-title" t-esc="story.title"/>
                </div>
                <div class="cs-story-body">
                    <div t-out="story.content[:300]"/>
                    <t t-if="len(story.content) > 300">
                        <span class="text-muted">...</span>
                    </t>
                </div>
            </div>
        </div>
    </t>
</template>

<!-- ============================================================================
     /my/documents — Legal documents and policies
     ============================================================================ -->

<template id="portal_documents" name="Legal Documents">
    <t t-call="portal.portal_layout">
        <t t-set="additional_title">Юридичні документи</t>

        <div class="cs-page">
            <div class="cs-page-back mb-3">
                <a href="/my" class="cs-back-link">← До кабінету</a>
            </div>

            <div class="cs-page-header mb-4">
                <h2 class="cs-page-title">Юридичні документи</h2>
                <p class="cs-page-subtitle">Умови, політики та згоди, які регулюють ваші відносини з CampScout</p>
            </div>

            <div class="row g-3 cs-doc-grid">
                <div class="col-md-6 col-lg-6">
                    <a href="/privacy-policy" target="_blank" class="cs-doc-card">
                        <span class="cs-doc-icon">🛡️</span>
                        <div class="cs-doc-body">
                            <h5 class="cs-doc-title">Обробка персональних даних</h5>
                            <p class="cs-doc-desc">Умови обробки даних, включаючи дані про дітей (RODO/GDPR)</p>
                            <span class="cs-doc-cta">Читати →</span>
                        </div>
                    </a>
                </div>
                <div class="col-md-6 col-lg-6">
                    <a href="/child-protection" target="_blank" class="cs-doc-card">
                        <span class="cs-doc-icon">🧒</span>
                        <div class="cs-doc-body">
                            <h5 class="cs-doc-title">Безпека дітей</h5>
                            <p class="cs-doc-desc">Політика захисту малолітніх і стандарти обслуговування в таборі</p>
                            <span class="cs-doc-cta">Читати →</span>
                        </div>
                    </a>
                </div>
                <div class="col-md-6 col-lg-6">
                    <a href="/regulamin-panelu-klienta" target="_blank" class="cs-doc-card">
                        <span class="cs-doc-icon">🔐</span>
                        <div class="cs-doc-body">
                            <h5 class="cs-doc-title">Умови користування кабінетом</h5>
                            <p class="cs-doc-desc">Правила користування батьківським особистим кабінетом</p>
                            <span class="cs-doc-cta">Читати →</span>
                        </div>
                    </a>
                </div>
                <div class="col-md-6 col-lg-6">
                    <a href="/terms" target="_blank" class="cs-doc-card">
                        <span class="cs-doc-icon">📄</span>
                        <div class="cs-doc-body">
                            <h5 class="cs-doc-title">Загальні умови</h5>
                            <p class="cs-doc-desc">Основні умови надання послуг табору CampScout</p>
                            <span class="cs-doc-cta">Читати →</span>
                        </div>
                    </a>
                </div>
                <div class="col-md-6 col-lg-6">
                    <a href="/cookie-policy" target="_blank" class="cs-doc-card">
                        <span class="cs-doc-icon">🍪</span>
                        <div class="cs-doc-body">
                            <h5 class="cs-doc-title">Політика cookies</h5>
                            <p class="cs-doc-desc">Які cookies ми використовуємо та навіщо</p>
                            <span class="cs-doc-cta">Читати →</span>
                        </div>
                    </a>
                </div>
            </div>

            <div t-if="documents" class="cs-doc-versions mt-5">
                <h5 class="mb-3">Актуальні версії документів</h5>
                <t t-foreach="documents" t-as="doc">
                    <div class="cs-doc-version">
                        <div class="cs-doc-version-head">
                            <h6 class="mb-1">
                                <t t-esc="dict([
                                    ('terms_conditions', 'Умови та положення'),
                                    ('privacy_policy', 'Політика конфіденційності'),
                                    ('rodo_consent', 'Згода RODO'),
                                    ('parental_consent', 'Батьківська згода')
                                ]).get(doc.document_type, doc.document_type)"/>
                            </h6>
                            <small class="text-muted">
                                Версія <t t-esc="doc.version_number"/> від <t t-esc="doc.version_date.strftime('%d.%m.%Y')"/>
                            </small>
                        </div>
                        <div class="cs-doc-version-body">
                            <div t-out="doc.content[:300]"/>
                            <t t-if="len(doc.content) > 300">
                                <span class="text-muted">…</span>
                            </t>
                        </div>
                    </div>
                </t>
            </div>
        </div>
    </t>
</template>

<!-- ============================================================================
     /my/loyalty — Loyalty program status
     ============================================================================ -->

<template id="portal_loyalty" name="Loyalty Program">
    <t t-call="portal.portal_layout">
        <t t-set="additional_title">Програма лояльності CampScout</t>

        <div class="cs-page">
            <div class="cs-page-back mb-3">
                <a href="/my" class="cs-back-link">← До кабінету</a>
            </div>

            <div class="cs-page-header mb-4">
                <h2 class="cs-page-title">Програма лояльності</h2>
                <p class="cs-page-subtitle">Чим більше таборів відвідала ваша дитина — тим вища знижка на наступні заїзди</p>
            </div>

            <!-- Per-child premium tier card -->
            <div t-foreach="loyalties" t-as="loyalty" class="cs-loyalty-card mb-4"
                 t-attf-data-tier="#{loyalty.loyalty_tier}">
                <div class="cs-loyalty-hero">
                    <div class="cs-loyalty-hero-inner">
                        <div class="cs-loyalty-child-name" t-esc="loyalty.participant_id.display_name"/>
                        <div class="cs-loyalty-tier-label">
                            Рівень
                            <strong>
                                <t t-esc="dict([
                                    ('bronze', 'Бронзовий'),
                                    ('silver', 'Срібний'),
                                    ('gold', 'Золотий'),
                                    ('platinum', 'Платиновий')
                                ]).get(loyalty.loyalty_tier, loyalty.loyalty_tier)"/>
                            </strong>
                        </div>
                        <div class="cs-loyalty-discount">
                            <t t-if="loyalty.discount_eligible">
                                <span class="cs-discount-value"><t t-esc="int(loyalty.discount_percentage)"/>%</span>
                                <span class="cs-discount-label">знижка на наступний табір</span>
                            </t>
                            <t t-else="">
                                <span class="cs-discount-label cs-no-discount">Знижка ще не активна</span>
                            </t>
                        </div>
                    </div>
                </div>

                <!-- Progress to next tier -->
                <div class="cs-loyalty-progress">
                    <t t-set="camps" t-value="loyalty.camp_count"/>
                    <t t-set="next_target" t-value="2 if camps &lt; 2 else (4 if camps &lt; 4 else (6 if camps &lt; 6 else None))"/>
                    <t t-set="next_name" t-value="'Срібного' if camps &lt; 2 else ('Золотого' if camps &lt; 4 else ('Платинового' if camps &lt; 6 else None))"/>
                    <t t-if="next_target">
                        <div class="cs-progress-label">
                            <span>До <strong t-esc="next_name"/> рівня залишилось
                                <strong t-esc="next_target - camps"/>
                                <t t-if="(next_target - camps) == 1">табір</t>
                                <t t-else="">табори</t>
                            </span>
                            <span class="text-muted"><t t-esc="camps"/>/<t t-esc="next_target"/></span>
                        </div>
                        <div class="cs-progress-bar">
                            <div class="cs-progress-fill" t-attf-style="width: #{(camps * 100) // next_target}%"/>
                        </div>
                    </t>
                    <t t-else="">
                        <div class="cs-progress-label cs-tier-max">
                            🏆 Максимальний рівень досягнуто
                        </div>
                    </t>
                </div>

                <!-- Stats row -->
                <div class="cs-loyalty-stats">
                    <div class="cs-stat">
                        <div class="cs-stat-value" t-esc="loyalty.camp_count"/>
                        <div class="cs-stat-label">Таборів відвідано</div>
                    </div>
                    <div class="cs-stat">
                        <div class="cs-stat-value" t-esc="loyalty.loyalty_points"/>
                        <div class="cs-stat-label">Балів зібрано</div>
                    </div>
                    <div class="cs-stat">
                        <div class="cs-stat-value">
                            <t t-if="loyalty.first_visit_date" t-esc="loyalty.first_visit_date.strftime('%Y')"/>
                            <t t-else="">—</t>
                        </div>
                        <div class="cs-stat-label">З нами з</div>
                    </div>
                </div>

                <t t-if="loyalty.badges">
                    <div class="cs-loyalty-badges">
                        <span class="cs-badges-label">Відзнаки:</span>
                        <span class="cs-badges-list" t-esc="loyalty.badges"/>
                    </div>
                </t>
            </div>

            <!-- Tier ladder / welcome info -->
            <div class="cs-tier-ladder mt-5">
                <h5 class="mb-3">Як працює програма</h5>
                <div class="cs-tier-row" data-tier="bronze">
                    <span class="cs-tier-pill">Бронзовий</span>
                    <span class="cs-tier-req">1 табір</span>
                    <span class="cs-tier-reward">знайомство</span>
                </div>
                <div class="cs-tier-row" data-tier="silver">
                    <span class="cs-tier-pill">Срібний</span>
                    <span class="cs-tier-req">2–3 табори</span>
                    <span class="cs-tier-reward">−5% знижка</span>
                </div>
                <div class="cs-tier-row" data-tier="gold">
                    <span class="cs-tier-pill">Золотий</span>
                    <span class="cs-tier-req">4–5 таборів</span>
                    <span class="cs-tier-reward">−10% знижка</span>
                </div>
                <div class="cs-tier-row" data-tier="platinum">
                    <span class="cs-tier-pill">Платиновий</span>
                    <span class="cs-tier-req">6+ таборів</span>
                    <span class="cs-tier-reward">−15% знижка</span>
                </div>
            </div>

            <div t-if="not loyalties" class="cs-empty-state mt-4">
                <div class="cs-empty-icon">🏆</div>
                <h4>Ласкаво просимо!</h4>
                <p class="text-muted">
                    Після першого табору вашої дитини тут з'явиться персональна картка лояльності
                    з реальними знижками на наступні заїзди.
                </p>
            </div>
        </div>
    </t>
</template>

</odoo>
