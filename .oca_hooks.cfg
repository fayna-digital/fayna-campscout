[MESSAGES_CONTROL]
# CampScout-модулі написані польською (pl_PL = source language).
# `po-pretty-format` видаляє self-translations (msgid == msgstr) як «noise»,
# але для source-мови це валідний паттерн — без нього Odoo фолбекне на en_US.
# Перевірено інцидентом 2026-05-06: --fix забрав 1352 pl_PL + 247 uk_UA перекладів.
disable = po-pretty-format
