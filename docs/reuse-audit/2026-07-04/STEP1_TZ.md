# ТЗ — Reuse-міграція, КРОК 1: злиття внутрішніх дублікатів

> Авторизація: «ок» власника 04.07 (§8 п.17 канону). Джерело: REUSE_AUDIT.md (розділ 🟡).
> Скоуп: ~1 282 LOC у 11 моделях → 5; БЕЗ зовнішніх залежностей і ліцензійних питань.
> Виконання: конвеєр лупа — ОДНА пара за ітерацію, кожна пара = окремий PR через CI.

## Контекст даних (знижує ризик)
Прод модуль НЕ встановлений [канон §10]. Живі дані лише на staging (тестові) та в
пісочниці campscout_rehearsal (не чіпається). Тому міграція даних = `migrations/`-скрипт
best-effort для staging; втрата тестових даних НЕ є блокером, але скрипт обов'язковий
(та сама механіка знадобиться на кроках 2-5, де дані будуть справжні).

## Пари (порядок = зростання ризику; keeper з аудиту, фінальний вибір — з grep-доказом лінків на ітерації)

| # | Мігрувати → у keeper | LOC | Примітка |
|---|---|---|---|
| 1 | ~~camp.stats.snapshot~~ → **camp.analytics.snapshot** ✅ 04.07 (PR#32) | 140 | total_capacity перенесено; бонус: полагоджено мертвий analytics-cron (домен цілив у неіснуючі поля — ValueError ковтався) |
| 2 | ~~camp.journal~~ → **camp.daily.report** ✅ 04.07 (PR#34) | 114 | keeper отримав active + 7р-архів-cron (у журналі метод був МЕРТВИЙ — без ir.cron); autofill кінцевого звіту перенаправлено на денні рапорти; записи → kierownik_notes, вкладення перепідвішені |
| 3 | camp.nutrition ⇄ camp.menu.day → **одна** | 131/104 | keeper за лінками kiosk/звітів |
| 4 | camp.participant.diet ⇄ camp.diet.profile → **одна** | 89/111 | keeper за лінками на pkt9 картки |
| 5 | camp.program(+activity) + camp.schedule.entry → **camp.program.structured(+day+line)** | 324 | structured-гілка канонічна (майстер її створює) |
| 6 | vozhatyi.training.record ⇄ fayna.vozhatyi.training(+module+certificate) ⇄ camp.staff.training.record → **одна** | 713 | найбільша; довгостроково — OCA hr_course (крок 4+) |

## Вимоги (EARS) — до КОЖНОЇ пари

- **[S1-1]** WHEN пара злита, THEN модуль ставиться ЧИСТО (`-i`, свіжа БД) без згадок видаленої моделі: 0 сиріт у views/ACL/menus/crons/record-rules/reports [verify: install-лог без ERROR + `git grep <модель>` = 0 поза migrations/].
- **[S1-2]** WHEN існуюча БД оновлюється (`-u`), THEN дані видаленої моделі перенесені у keeper `migrations/`-скриптом (мапінг полів у скрипті задокументований), а залишкові ir.model.data вичищені [verify: upgrade-лог + контрольний SELECT count у скрипті].
- **[S1-3]** WHEN пара злита, THEN повний тест-сюїт зелений; тести видаленої моделі перенесені на keeper (НЕ видалені мовчки) [verify: харнес 0 failed; diff тестів у PR].
- **[S1-4]** WHEN keeper поглинає поля, THEN унікальна семантика видаленої моделі збережена (поля/compute переносяться, не губляться) — розбіжності явно перелічені в PR-описі [verify: рев'ю таблиці полів у PR].
- **[S1-5]** Канон: TZ §8 п.17 і REUSE_AUDIT позначають пару виконаною; CHANGELOG-запис [verify: docs-diff у тому ж PR].

## DoD кроку 1 (весь)
Усі 6 пар влиті; сума видаленого коду ≥600 LOC; повний сюїт зелений; staging задеплоєний
(бамп версії за [PR-4]); власник бачить незмінні екрани (kiosk/портал працюють на keeper-моделях).

## Поза скоупом кроку 1
Заміни на стандарт/OCA (кроки 2-5), будь-які нові фічі, зміни UX.
