# План покращення ТЗ — /Users/kobzar/Developer/Fayna-Workspace/Projects/fayna_camp_portal/docs/TZ.md

_Дата: 2026-07-03. Кожен рядок: знахідка → готове EARS-формулювання з методом верифікації → пріоритет MoSCoW. Правки застосовує людина/агент окремим кроком._

| Вимога | Проблема | Нове формулювання (EARS) | Верифікація | Пріоритет |
|---|---|---|---|---|
| B-1 | Відсутні кількісні показники для оцінки успіху вирішення проблеми (напр., % зменшення часу/витрат, % підвищення комплаєнсу). | IF the portal automates MEN-документація, RODO, and Kamilka-протоколи processes, THEN the average time spent by staff on these tasks SHALL decrease by at least 50% compared to manual processes. | analyze | Must |
| B-1 | Метод верифікації відсутній, зазначено 'не застосовно', що суперечить принципу верифікованості вимог. | IF the portal automates MEN-документація, RODO, and Kamilka-протоколи processes, THEN the total operational cost associated with these processes SHALL decrease by at least 30% within 6 months of full production deployment. | analyze | Must |
| B-3 | Зазначено, що 'сам cutover не виконано', що вказує на невиконання критичних термінів і ставить під сумнів поточну реалістичність вимоги. | The portal SHALL be fully deployed to production and operational for new season registrations by August 1, 2026. | inspect | Must |
| B-5 | Вимога 'Кожен екран самозрозумілий без інструкції' є нечіткою та не вимірною, відсутні кількісні критерії успіху (напр., % успішних завдань без допомоги). | WHILE interacting with the portal, 90% of first-time users SHALL successfully complete core tasks (e.g., registration, profile update) without referring to external documentation or requiring assistance. | test | Must |
| B-5 | Вимога поєднує дві різні нефункціональні характеристики (юзабіліті та двомовність), що порушує принцип сингулярності. | The portal SHALL support both Ukrainian (UA) and Polish (PL) languages for all user-facing text content, with Polish as the default language. | inspect | Must |
| B-5 | Відсутній явний метод верифікації для обох частин вимоги. Для двомовності зазначено 'CI червоний', що вказує на поточну проблему з реалізацією. | WHEN a user interacts with any screen, THEN its purpose and functionality SHALL be self-explanatory without external instructions, AND the system SHALL support both Ukrainian (UA) and Polish (PL) languages, with Polish (PL) as the default. | Demo with new users for UX clarity; Test language switching (UA/PL) and verify default (PL). | Must |
| B-6 | Це не вимога до системи, а бізнес-рішення/обмеження проєкту. Має бути в розділі 'Бізнес-контекст і цінність' або 'Пріоритезація'. | WHEN the system is developed, THEN it SHALL prioritize the implementation of features required for CampScout's seasonal operations, AND SHALL defer all white-label resale functionalities (e.g., SMS debraiding, data-to-demo) until a specific business decision to proceed is made. | Inspect project backlog and feature roadmap for alignment with CampScout priority; Inspect system for absence of white-label features. | Must |
| B-6 | Відсутність чітких критеріїв верифікації та вимірності для 'портал працює' та 'відкладено'. | WHEN CampScout users perform seasonal operations (e.g., registration, payment), THEN the portal SHALL be available and fully functional with an uptime of at least 99.5% during business hours. IF white-label functionality is considered, THEN the system SHALL NOT contain any features for white-label resale, including SMS debraiding or data-to-demo conversion, until a business decision to proceed is made. | Analyze system uptime logs and incident reports; Test core CampScout workflows; Inspect code and UI for absence of specified white-label features. | Must |
| B-6 | Порушує Singular: містить кілька окремих рішень/пріоритетів в одній вимозі. | WHEN CampScout operates during its season, THEN the portal SHALL provide all necessary functionalities for their operations (e.g., registration, payment processing) with an availability of at least 99.5% during business hours. | Analyze system uptime logs; Test core CampScout workflows. | Must |
| A-4 | Вимога порушує Singular, об'єднуючи аспекти надійності інтеграції та якості збірки (CI-процеси). Рекомендовано розділити. | WHEN the system integrates with external services (e.g., sale, event, registration, account), THEN it SHALL handle integration failures using try/except blocks, ensuring that such failures DO NOT block sales or payment processing. | Test integration failure scenarios by simulating external service unavailability; Inspect code for try/except implementation. | Must |
| A-5 | Вимога не є сингулярною, поєднуючи кілька окремих критеріїв build-гейту та бізнес-рішення в одному твердженні. | WHEN a build is performed, THEN the dependency tree MUST contain zero (0) AGPL-licensed packages. | Analyze (automated dependency license scan). | Must |
| A-5 | Фраза «чисті межі модулів» є неоднозначною та не має чіткого, об'єктивного визначення або методу верифікації. | WHEN a build is performed, THEN the module dependency graph MUST contain zero (0) circular dependencies between modules. | Analyze (automated static code analysis for dependency cycles). | Must |
| P-2 | Виявлено критичну невідповідність: файли LICENSE містять текст LGPL, тоді як manifest вказує OPL-1, що створює значний юридичний ризик. | WHEN a module is released, THEN the content of its `LICENSE` file MUST exactly match the text of the OPL-1 license for all modules, including `fayna_rodo_compliance` and `fayna_sms_base`. | Inspect (manual review or automated hash comparison of `LICENSE` files). | Must |
| P-2 | Вимога включає історичний контекст («Co-Authored-By: Claude»), який не є перспективною вимогою. | GOING FORWARD, WHEN new code is committed, THEN it MUST NOT include "Co-Authored-By: Claude" or any other AI co-authorship attribution in commit messages. | Inspect (code review of commit messages). | Must |
| P-3 | Вимога змішує опис поточного стану з майбутньою роботою, що ускладнює ідентифікацію фактичної вимоги. | WHILE providing SMS functionality, THEN the system MUST support at least three (3) distinct SMS providers (e.g., TurboSMS, SMSAPI.pl, SerwerSMS.pl) via a provider-agnostic interface, allowing dynamic routing and balance management per adapter. | Test (implement and verify integration with multiple providers using mock HTTP tests). | Should |
| P-3 | «Роутинг + баланс per адаптер» є розпливчастим і не має конкретних критеріїв для визначення повної або успішної реалізації. | WHEN multiple SMS provider adapters are configured, THEN the system SHALL route SMS messages based on `provider_type` and distribute the load across available adapters to ensure efficient delivery, supporting at least 3 distinct provider types. | Automated tests SHALL mock HTTP responses for each adapter type to verify correct routing and load distribution logic. | Could |
| R-2 | Verification method "experiential-прогони" is informal and lacks repeatability, making it difficult to ensure full coverage and consistent validation. | WHEN a user logs in with a defined Kiosk role (e.g., instructor, parent), THEN the system SHALL display only the Kiosk interface with operations relevant to that role, and restrict access to full Odoo features. | Automated UI tests SHALL be implemented for each Kiosk role to verify that only authorized Kiosk operations are accessible and full Odoo access is denied. | Should |
| R-3 | The recursive hierarchical view component (14-TZ §5.1) is not implemented, impacting the full realization of the "pyramid of visibility" concept. | WHEN a user views hierarchical data (e.g., event, group, educator, children), THEN the system SHALL render an OWL-based recursive tree view component that dynamically adjusts its depth and content based on the user's record rules and entry level. | Demo SHALL be conducted to showcase the hierarchical view for different roles (e.g., parent, educator, leader) to confirm correct data representation and access. | Could |
| R-4 | Attachment-ACL on PDF is a critical security gap (F-RODO-2) that needs to be addressed to ensure full data protection and RODO compliance. | WHEN a user attempts to access a child's PDF card, THEN the system SHALL enforce attachment-level access control, allowing viewing or downloading only for roles explicitly authorized by the RODO matrix (e.g., parent for their child, medic for all camp children). | Automated security tests SHALL be implemented to verify that unauthorized roles cannot access PDF attachments. | Must |
| R-4 | While access rules are defined, the incomplete verification of PDF ACLs means the requirement is not fully met or verified. | WHILE access rules for child's PDF cards are defined, THEN the system SHALL ensure that all PDF attachments are subject to a robust, verifiable ACL mechanism that prevents unauthorized access, aligning with RODO requirements. | An independent security audit SHALL be conducted to confirm full RODO compliance for PDF attachment access, supplementing automated tests. | Must |
| R-5 | The terminology layer is not implemented, hindering the system's "sellable" characteristic (A-5.2) and readiness for the SaaS stage. | WHEN an organizer configures their profile, THEN the system SHALL allow them to define custom labels for roles (e.g., "молода кадра", "гурткові", "kierownik", "komendant") based on their organization type. WHILE displaying role names to users, THEN the system SHALL use the custom labels configured by the organizer, retrieved via a `_get_role_label()` helper, instead of hardcoded values. | Demo (configure labels and verify display), Test (unit tests for `_get_role_label` and integration tests for label persistence and display). | Must |
| R-5 | The requirement is currently not verifiable as the feature is not implemented, lacking any proof of concept or test coverage. | WHEN an organizer configures their profile, THEN the system SHALL allow them to define custom labels for roles (e.g., "молода кадра", "гурткові", "kierownik", "komendant") based on their organization type. WHILE displaying role names to users, THEN the system SHALL use the custom labels configured by the organizer, retrieved via a `_get_role_label()` helper, instead of hardcoded values. | Demo (configure labels and verify display), Test (unit tests for `_get_role_label` and integration tests for label persistence and display). | Must |
| F-WIZ-3 | Workflow description lacks details on rejection path, failure scenarios during cascade creation, or un-approval. | IF an organizer's submission is rejected, THEN the system SHALL revert the `camp_approval_state` to 'draft' and prevent the cascade creation of associated entities. IF the cascade creation of entities (event, ticket, product, program, groups, budget, teczka) fails during the 'approve' action, THEN the system SHALL log the failure, maintain the `camp_approval_state` as 'submitted', and provide an administrative mechanism to retry or manually resolve the creation. IF an approved camp is subsequently un-approved, THEN the system SHALL set `website_published=False` for the associated event and … | Test (add specific test cases for rejection, cascade failure, and un-approval scenarios), Demo (demonstrate rejection and un-approval flows). | Must |
| F-WIZ-5 | Requirement describes two distinct functionalities (group creation and child assignment) that could be separated for clarity and independent testing/prioritizat | WHEN a camp is approved and its cascade creation is triggered, THEN the system SHALL automatically create groups for the camp. IF the total number of seats is `S`, THEN the system SHALL create `ceil(S/20)` groups. WHILE creating groups, THEN the system SHALL apply the following constraints: A group SHALL contain a maximum of 20 children. IF a group contains a child under 10 years old, THEN it SHALL contain a maximum of 15 children. A group SHALL contain a maximum of 2 children with disabilities (`niepełnosprawni`). WHEN children are reserved for a camp, THEN the system SHALL assign them to … | Test (existing `test_camp_group.py` and `test_phase_d_split.py` should cover this, but new tests might be needed for specific constraint edge cases). | Must |
| F-WIZ-7 | Відсутній чіткий метод верифікації та критерії приймання (DoD) для тригерів та механізмів примусу. | WHEN an event is identified as 'high-risk' (either by `is_high_risk` boolean or by an activity type trigger like 'skiing' or 'water sports'), THEN the system SHALL automatically create an instructor vacancy for that event. WHILE creating an instructor vacancy for a high-risk event, THEN the system SHALL mark the vacancy as requiring a valid license/certification. WHILE creating or editing a high-risk event, THEN the system SHALL enforce the attachment of mandatory insurance documentation. | Test (unit/integration tests for `is_high_risk` trigger, auto-vacancy creation, license requirement, and insurance attachment validation), Demo (create a high-risk event and verify the triggered actions). | Must |
| F-WIZ-7 | Вимога об'єднує кілька функціональностей (прапор, тригер, вимоги до інструктора, страховки), що порушує Singular. | WHEN an event is marked as 'high-risk' (e.g., via activity type or explicit flag), THEN the system SHALL automatically create an instructor vacancy requiring a valid license/certification AND require an insurance attachment for the event. | Test: Create a high-risk event, verify auto-generated instructor vacancy with license requirement and mandatory insurance attachment field. | Must |
| F-WIZ-7 | Умова 'activity-тип-тригер' є нечіткою; відсутній повний перелік high-risk активностей. | WHEN an event's activity type is selected, THEN the system SHALL automatically identify it as 'high-risk' IF the activity type is present in the predefined list of high-risk activities (e.g., 'Skiing', 'Water Sports', 'Mountaineering'). The list SHALL contain at least 2 entries. | Inspect: Verify the configuration/lookup table for high-risk activity types. Test: Create events with high-risk and non-high-risk activities to confirm correct identification. | Must |
| F-WIZ-8 | Опис 'Крок «Фото»' є надто загальним; відсутні деталі щодо функціональності кроку (наприклад, завантаження кількох фото, обмеження). | WHEN a user reaches the 'Photo' step in the event creation wizard, THEN the system SHALL allow uploading up to 5 images for the event gallery, automatically resizing them to a maximum width of 1920 pixels and converting them to WebP format, AND SHALL use the first uploaded image as the primary for website meta tags (og:image) and alt text. | Test: Upload 1, 3, and 5+ images, verify resizing, WebP format, and correct meta tag population on the website. | Must |
| F-PRG-2 | Опис 'запасна програма' є неповним; незрозуміло, як саме створюється та керується дощовий план (структура, взаємодія з основним планом). | WHILE creating an event program, IF the user marks a program as 'is_rain_plan', THEN this program SHALL be designated as an alternative, AND both the primary and the designated rain plan SHALL be visible on the event's product card, clearly indicating their roles. | Demo: Create an event with a primary and a rain plan. Inspect the product card to confirm both programs are displayed with clear labels. | Must |
| F-PRG-3 | Визначення '100% заповнення' нечітке, не вказано, що саме має бути заповнено та як вимірюється повнота. Нечітко описано 'аварійна/запасна програма'. | WHEN a camp program (including any designated 'rain plan' program) is being finalized, THEN the system SHALL calculate its completion percentage based on all required program fields (e.g., daily schedule, activities, assigned instructors), AND IF the completion is less than 100%, THEN the system SHALL prevent the camp from being started with a blocking message. | Test: Attempt to start a camp with an incomplete program (primary or rain plan) and verify the blocking mechanism. Inspect: Review the logic for calculating program completion based on at least 5 required fields. | Must |
| F-REC-3 | Повний пакет документів виховника не наповнено на тесті, що вказує на прогалину у повноті реалізації та верифікації вимоги. | WHEN a 'wychowawca' (escort staff) record is created in 'camp.staff', THEN the system SHALL provide a checklist for the required documents (CV, анкета, umowa zlecenia, евіденція годин, рахунок) and allow their upload. All 5 specified documents MUST be uploaded and linked for the staff member to be considered fully documented. | Inspect a 'camp.staff' record for a 'wychowawca' in the test environment to confirm all 5 required documents are present and linked. | Must |
| F-REC-4 | Requirement combines multiple distinct functional requirements (rates, vacancy fields, cancellation logic), violating singularity. Each should be a separate req | WHEN calculating staff costs, THEN the system SHALL use the following daily rates: 'wychowawca' 220 PLN, 'kierownik' 260 PLN, and 'instruktor' 300 PLN. | Test the staff cost calculator with each role to ensure the correct daily rate is applied. | Must |
| F-REC-4 | Verification status is unclear for a significant part of the requirement ('не верифіковано'), impacting overall verifiability and completeness. | WHEN a participant registration is cancelled, THEN the system SHALL NOT automatically close any associated hired staff positions, but instead SHALL set an 'overstaffing' flag on the staff record for manual review. | Test by cancelling a participant registration associated with a hired staff member and inspect the staff record to confirm the position remains open and the 'overstaffing' flag is set. | Must |
| F-KKW-2 | Requirement bundles several distinct functional behaviors and constraints (signing, card uniqueness, data copying, pre-filling). Violates singularity. | WHEN a parent signs a participant card via the portal at '/my/participants/<id>', THEN the system SHALL capture the signature as a binary attachment, record the 'signed_date', 'signed_ip', 'signed_by' user, and log the event in the RODO audit trail. | Test the signing flow on the portal by a parent user, then inspect the participant record and RODO log to confirm all specified data points are correctly captured. | Must |
| F-KKW-3 | One of the three specified non-repudiation mechanisms (PDF hash) is explicitly marked as 'не підтверджено кодом', indicating incomplete implementation/verificat | WHEN a participant card PDF is signed, THEN the system SHALL calculate a SHA-256 cryptographic hash of the signed PDF content and store this hash in a 'signed_pdf_hash' field associated with the participant record. | Inspect the participant sign-flow code to confirm the hash calculation and storage. Test by signing a PDF and verifying the 'signed_pdf_hash' field contains a valid hash of the signed document. | Must |
| F-KKW-4 | The core constraint of the requirement ('сам constraint') is explicitly marked as 'не верифіковано тестом', indicating incomplete implementation or testing. | WHEN a user attempts to register a child with 'hydrophobia' or 'fear_of_heights' for a water-based or height-related activity, THEN the system SHALL display a ValidationError and prevent registration. | test | Must |
| F-KKW-5 | Порушення Singular: вимога описує дві окремі згоди (4a, 4b) з різними умовами, що краще розділити на окремі вимоги. | WHEN a parent provides consent for image usage (4a), THEN the system SHALL record the consent with a canvas signature, IP address, and a RODO log entry. WHILE a child's photo is used in stories, THEN the system SHALL verify the existence of the image consent (4a). | test | Must |
| F-KKW-5 | Порушення Singular: вимога описує дві окремі згоди (4a, 4b) з різними умовами, що краще розділити на окремі вимоги. | WHEN a parent provides or revokes consent for marketing purposes (4b), THEN the system SHALL allow the parent to toggle this consent at any time, and record the action in a RODO log. | test | Must |
| F-KKW-6 | Verifiable: Автоматичний cron-механізм не верифіковано, що є критичним для функціоналу. | WHEN a child's registration lacks a signed card or RODO consent less than 24 hours before the activity start time, THEN the system SHALL automatically cancel the registration, process a full refund, and record the action in a RODO log. | test | Must |
| F-KKW-6 | Spec-drift: Потенційна відсутність або невідповідність реалізації cron-завдання у коді. | The system SHALL include a scheduled cron job that executes daily to identify and process registrations lacking signed cards or RODO consent less than 24 hours before activity start. | inspect/test | Must |
| F-KKW-7 | Unambiguous: 'Фірмовий еталон' не знайдено, що робить вимогу нечіткою та суб'єктивною ('гарніший'). | WHEN the system generates a PDF 'karta_report', THEN the report SHALL adhere to the visual styling and branding specified in the 'camp / fayna_camp_qualification / Dokumenty' repository, including the company logo and details from res.company. | inspect/demo | Should |
| F-KKW-7 | Verifiable: Відсутність еталону унеможливлює об'єктивну верифікацію відповідності PDF. | WHEN a Karta PDF is generated, THEN its visual appearance MUST conform to the approved corporate standard, including brand, logo, and requisites from res.company. | Inspect (visual comparison against the provided standard in `fayna_camp_qualification/Dokumenty`). | Must |
| F-KKW-7 | has_verification_method: Метод верифікації не може бути застосований без визначеного еталону. | WHEN an approved corporate standard for the Karta PDF is available, THEN the system MUST enable objective verification of generated Karta PDFs against this standard. | Demo (show the comparison process) / Inspect (compare a generated PDF to the standard). | Must |
| F-RODO-2 | Verifiable: Другий механізм (attachment-ACL) не підтверджено тестом, а тести HTTP-ізоляції 'ЧЕРВОНІ'. | WHEN a user without Art.9 access attempts to open a Karta PDF containing medical data via direct URL, THEN the system MUST prevent access to the PDF (0 successful attempts for unauthorized users). | Test (automated test simulating unauthorized access via direct URL). | Must |
| F-RODO-2 | Spec-drift: 3 червоні тести в PR #15 вказують на серйозну розбіжність між ТЗ та кодом/тестами. | IF `test_art9_http_isolation.py` contains failing tests (e.g., 3 red tests in PR #15), THEN the system's HTTP isolation for Art.9 data MUST be resolved to ensure a 100% test pass rate. | Analyze (review test results and code) / Test (re-run tests after fix). | Must |
| F-RODO-2 | Completeness: Необхідність 'з'ясувати' причину червоних тестів вказує на неповне розуміння проблеми. | WHEN `test_art9_http_isolation.py` tests fail, THEN the root cause (e.g., actual security vulnerability or outdated test logic) MUST be identified and documented with 100% clarity. | Analyze (investigate code, configuration, and test logic). | Must |
| F-RODO-3 | Verification method (test for blocking) is explicitly noted as missing, impacting verifiability. | WHEN a user attempts to access participant cards for the first time in a 12-month period, IF they have not confirmed the declaration, THEN the system SHALL display a blocking modal and prevent access to participant cards. | Test (simulate access without declaration, verify blocking; simulate access after declaration, verify access). | Must |
| F-RODO-4 | Verification methods (tests, SendPulse sync verification) are explicitly noted as missing. | WHEN a user initiates the erasure wizard for a res.partner record, THEN the system SHALL anonymize all non-legal data, log the action as 'erased', and synchronize the anonymization status with SendPulse within 60 seconds. Legal data (e.g., account.move, sale.order) SHALL be anonymized but retained for 5-7 years, while medical/incident data SHALL NOT be erased. | Test (execute wizard, verify data anonymization in DB, verify log entry, verify SendPulse API call/status). | Must |
| F-RODO-7 | Requirement is a composite of multiple distinct NFRs (singular violation), making it difficult to track and verify as one unit. | WHEN handling data subject to art.9 or declaration, THEN the system SHALL ensure that all data stored at rest in the filestore is encrypted. (Note: This is one atomic requirement extracted from the composite F-RODO-7; other NFRs should be similarly split). | Inspect (infrastructure configuration for filestore encryption, e.g., LUKS/volume encryption). | Must |
| F-RODO-7 | Several NFRs are vague (e.g., 'шифрування at-rest', 'retention', 'повний audit', 'ротація секретів') lacking specific mechanisms or quantifiable thresholds. | WHEN handling data subject to art.9 or declaration, THEN the system SHALL ensure that all data stored at rest in the PostgreSQL database is encrypted using AES-256. | Inspect (PostgreSQL configuration for pgcrypto or underlying volume encryption settings). | Must |
| F-RODO-7 | Many aspects are not measurable (e.g., 'retention' period, 'повний audit' scope, 'ротація секретів' frequency). | WHEN handling data subject to art.9 or declaration, THEN the system SHALL ensure that all application secrets (e.g., database passwords, API keys) are rotated automatically at least every 90 days. | Inspect (secret management system configuration and rotation logs). | Must |
| F-KAM-1 | Неоднозначність у визначенні 'ВСІХ subscribers' та 'без прочитання', що критично для обробки інцидентів. | WHEN a `camp.incident.report` of severity 'kamilka' is triggered, THEN the system SHALL send an SMS notification to all active subscribers registered in the system, overriding any general opt-out preferences as per GDPR art.6.1.d. Furthermore, 'unacknowledged' SHALL be defined as the absence of a user-initiated confirmation action (e.g., link click, system acknowledgement) within the specified timeframe. | Test | Must |
| F-KAM-1 | Відсутній прямий тест для верифікації ескалації через 5 хв без прочитання, що є відомим боргом P3. | The system SHALL include an automated test case that verifies the incident escalation process: WHEN a critical incident SMS is sent and remains unacknowledged for exactly 5 minutes, THEN the system SHALL successfully trigger an escalation to the designated backup contact. | Test | Must |
| F-KAM-1 | Вимога об'єднує кілька окремих функцій (SMS, ескалація, лог), що порушує принцип Singular. | WHEN a `camp.incident.report` of severity 'kamilka' is triggered, THEN the system SHALL send an SMS notification to all active subscribers registered in the system, overriding any general opt-out preferences as per GDPR art.6.1.d. | Test | Must |
| F-KAM-2 | Знахідка тесту 02.07: п.9 'opis' у згенерованому документі порожній, що вказує на некоректну реалізацію критичного документа. | WHEN the 'Karta Wypadku' document is generated, THEN field §11, point 9 ('opis') SHALL be correctly populated with the incident description from the system's `camp.incident.card` data, ensuring it is never empty. | Test | Must |
| F-KAM-2 | Вимога описує дві тісно пов'язані, але окремі сутності (Karta Wypadku та Rejestr Wypadków), що може бути розділено. | The system SHALL generate a 'Karta Wypadku' document that adheres to §11, containing exactly 16 distinct data points, formatted for official submission. | Inspect | Must |
| F-KAM-3 | SLA-таймери та список порушень не верифіковано, що є критичним для протоколу інциденту. | WHEN an incident occurs and progresses through its workflow, THEN the system SHALL accurately calculate and display the current SLA status, including any breaches, according to the defined timeframes (e.g., 0-10s scene safety, 0-2min CPR, 2-15min safe place/ambulance/parent contact). | Test (unit tests for `emergency.py` covering `sla_*` fields and computed booleans, integration tests for dashboard display). | Must |
| F-KSK-2 | Вимога є пакетом з багатьох окремих UX-покращень, що порушує принцип Singular та ускладнює відстеження, пріоритезацію та тестування. | WHEN the Kiosk interface is accessed on a desktop device with a screen width of 1024 pixels or more, THEN the system SHALL display the content using a 3x3 grid, compact tiles, and a centered layout with a maximum width. | Inspect (manual verification on 1440px and 390px screens, ensuring no orphaned elements or empty halves). | Should |
| F-KSK-2 | Відсутність індивідуальних ідентифікаторів для під-вимог ускладнює трасування та звітність по кожному елементу UX-ремонту. | WHEN a user accesses the Kiosk or `/my` interface, THEN the system SHALL display the CampScout brand logo (wordmark from brandbook) in the header. | Inspect (manual verification of the header on Kiosk and `/my` pages). | Should |
| F-KSK-3 | Requirement combines multiple distinct functionalities (deadline, status, checklist, indicators, notifications), violating singularity and making it hard to tra | WHILE a camp is in `draft` or `submitted` status, THEN the system SHALL display the Kuratorium deadline date (21 days for national, 14 days for international camps) and a real-time countdown to that deadline on the per-camp status panel. | Test (unit tests for `deadline_date` calculation and countdown logic), Inspect (manual verification of the displayed date and countdown). | Must |
| F-KSK-3 | Vague description of 'документи ПОДАТИ' and 'наскрізні індикатори' lacks specific details for implementation and verification. | WHILE a camp is active, THEN the system SHALL display a 'Recruitment Closed' indicator on the per-camp status panel IF the number of registered participants for the camp equals or exceeds the defined maximum capacity. | Test (unit tests for participant count vs. capacity logic), Inspect (manual verification of the indicator's state based on participant data). | Should |
| F-KSK-3 | Verification method is mentioned as 'суміжно' but doesn't cover all aspects, especially dynamic indicators and notifications. No clear measurable thresholds. | WHEN a camp manager accesses their per-camp status panel, THEN the system SHALL display: the Kuratorium deadline (21 days for domestic, 14 for international), a countdown to this deadline, the camp's status (draft, submitted, registered), a checklist of required documents with their submission status, and cross-cutting indicators for payments (e.g., 'Paid: X%'), recruitment (e.g., 'Closed: Y/Z'), program completion (e.g., 'Progress: 100%'), insurance status, and §13 verification status. WHILE any deadline is within 7 days or any required document is not submitted, THEN the system SHALL … | Test: Verify panel displays correct deadlines, countdowns, statuses, and indicator values for various camp states (e.g., draft, submitted, registered, overdue documents, 50% recruitment, 100% program). Inspect: Confirm mail.activity/SMS reminders are generated based on defined thresholds. | Must |
| F-KSK-4 | This requirement is a high-level feature description rather than a specific, verifiable requirement. It lacks detail on scope, specific obligations, and trackin | WHEN a camp manager accesses their cabinet, THEN the system SHALL present a consolidated process tracker displaying the status of all mandatory camp preparation obligations, including (but not limited to) KRK collection, submission to Kuratorium, Sanepid/PSP, child recruitment stages, and staff interviews. IF any obligation is overdue or its deadline is within 7 days, THEN the system SHALL display a visual alert on the tracker and generate an automated reminder (email/SMS). | Demo: Show the consolidated tracker with various obligation statuses (pending, completed, overdue). Test: Verify reminders are triggered correctly based on defined deadlines. | Must |
| F-KSK-4 | It heavily relies on F-KSK-3 but doesn't clearly define its own unique scope or how it extends F-KSK-3, leading to potential overlap and ambiguity. | WHILE the F-KSK-3 status panel provides a high-level overview of camp progress, THEN the F-KSK-4 Manager's Cabinet SHALL extend this by offering a consolidated, interactive process tracker that allows managers to view, update, and manage the status of individual obligations, including detailed steps for document submissions (e.g., KRK, Kuratorium, Sanepid/PSP), child recruitment stages, and staff interviews, ensuring clear separation of high-level status from granular task management. | Demo: Show how F-KSK-4 provides more granular control and tracking for items summarized in F-KSK-3. Inspect: Review UI/UX to ensure clear distinction and logical flow between the two features. | Must |
| F-KSK-4 | No specific verification method or measurable criteria are provided, making it impossible to confirm implementation. | WHEN a camp manager accesses their cabinet, THEN the system SHALL display a process tracker listing all defined camp preparation obligations (e.g., KRK collection, Kuratorium submission, Sanepid/PSP, child recruitment, staff interviews). FOR EACH obligation, the system SHALL display its current status (e.g., 'Not Started', 'In Progress', 'Completed', 'Overdue') and its deadline. IF an obligation's deadline is within 7 days or is overdue by more than 0 days, THEN the system SHALL visually highlight the obligation and generate an email/SMS reminder. | Test: Create test cases for each obligation type, verifying status updates, deadline calculations, visual highlighting, and reminder generation (e.g., check mail.activity logs, SMS gateway logs). Demo: Present the tracker with various states and demonstrate reminder functionality. | Must |
| F-MY-4 | Вимога об'єднує дві різні функціональності ('Фото дня' та 'Погода дня'), що порушує принцип Singular. Потребує розділення на окремі вимоги. | WHEN an educator uses their mobile device to capture a photo, THEN the system SHALL allow them to upload it to the camp's story. WHEN a parent views their child's camp story, THEN the system SHALL display the 'Photo of the Day' for their specific camp session, provided parental consent is recorded via `consent-gate`. The photo SHALL be displayed within 3 seconds of page load. | Test: `test_story_photo_consent_gate.py`. Demo: Show educator uploading a photo and parent viewing it on the portal. Inspect: Verify photo loads within 3 seconds. | Must |
| F-MY-4 | Частина вимоги ('погода-день public-зріз і мобільний upload') явно зазначена як 'не верифіковано', що вказує на відсутність критеріїв верифікації. | WHEN a parent views their child's camp story, THEN the system SHALL display the 'Weather of the Day' as a public-facing section of the `daily.report`, ensuring that no sensitive or internal fields are exposed. The weather information SHALL be updated daily by 10:00 AM local time. | Inspect: Verify `daily.report` portal render for public-facing weather data. Test: Confirm no internal fields are visible. Analyze: Check update timestamp for weather data. | Should |
| F-MY-4 | Частина вимоги ('погода-день public-зріз і мобільний upload') явно зазначена як 'не верифіковано', що вказує на відсутність критеріїв верифікації. | WHEN a daily report is published, THEN parents SHALL be able to view a public weather summary on the camp story portal. WHEN an educator captures a photo, THEN they SHALL be able to upload it via mobile and see a preview. | Demo/Inspect portal render of daily.report; Test mobile upload flow and preview display. | Must |
| F-MY-5 | Вимога не повністю визначає механізм верифікації для UI-частини ('окремої сторінки preferences не знайдено'), що ускладнює її тестування. | IF a parent navigates to /my/account or consents, THEN they SHALL find a UI block to opt-out of SMS notifications (both general and marketing), while retaining email/push. | Demo/Inspect UI for SMS opt-out block in /my/account or consents; Test opt-out functionality. | Should |
| F-COM-1 | Хоча згадано 'Тести: у test-gate', прямо вказано на прогалину в тестуванні 'cost-guard', що знижує повну верифікованість вимоги. | IF SMS cost-guard policies are defined (e.g., <50 chars, no PII), THEN an automated test SHALL exist to verify adherence to these policies for all outgoing SMS messages. | Test (automated test execution of cost-guard scenarios). | Must |
| F-COM-2 | Вимога чітко визначає ліміти, але прямо вказує на відсутність автоматизованого тесту на ліміт, що знижує верифікованість. | IF SMS sending limits are defined (100/month for educators, 300/month for managers), THEN an automated test SHALL exist to verify that users cannot exceed these limits. | Test (automated test execution for limit enforcement). | Must |
| F-COM-3 | Вимога прямо вказує, що 'повна матриця auto-subscribe по 8 моделях не верифікована', що є значною прогалиною у верифікації та повноті. | WHEN a new event or record is created for any of the 8 defined models (e.g., card, incident, story), THEN the system SHALL automatically subscribe the correct predefined set of users to its associated discussion channel. | Inspect/Analyze auto-subscribe logic and configuration for all 8 models; Test specific auto-subscription scenarios. | Must |
| F-COM-3 | Хоча є приклади, повний перелік '8 моделей' та їхні правила підписки не описані, що знижує повноту вимоги. | WHEN a new event channel is created for any of the 8 defined models, THEN the system SHALL automatically subscribe the predefined users/roles according to the specific rules for that model. | Test: Create channels for each of the 8 models and verify that the correct users/roles are automatically subscribed. Inspect: Review documentation for the auto-subscription matrix for all 8 models. | Must |
| F-COM-4 | Відсутній чіткий метод верифікації та критерії приймання для DM-чату та RODO-сліду. | WHEN a direct message is sent between a wychowawca and a parent, THEN the system SHALL log the full content of the message, sender, recipient, and timestamp, ensuring it is retrievable for RODO and legal audit purposes for a minimum of 5 years. | Test: Send multiple DM messages, then request and verify the completeness and retrievability of logs for content, sender, recipient, timestamp. Analyze: Review audit log retention policies and data access controls. | Must |
| F-COM-4 | Недостатньо деталізовані функціональні вимоги до чату (наприклад, типи повідомлень, вкладення, сповіщення). | WHILE a wychowawca or parent is using the DM chat, THEN the system SHALL allow sending and receiving text messages, and SHALL support attaching up to 3 files per message with a maximum size of 5MB per file. | Test: Send text messages, and attempt to attach 1, 2, 3, and 4 files (each <5MB) to a message. Attempt to attach a single file >5MB. Verify successful sending/receiving within limits and rejection beyond limits. | Should |
| F-COM-4 | Механізм RODO art.9 gate описаний як ADR, а не як конкретна вимога, що може призвести до неоднозначності реалізації. | WHEN a user attempts to send a message containing predefined sensitive keywords related to RODO Article 9 (e.g., "ліки", "хвороба", "алергія") in the DM chat, THEN the system SHALL display a warning message and require explicit confirmation or offer to mask the sensitive information before sending. | Test: Attempt to send messages containing various RODO Article 9 keywords. Verify that the warning message appears and that the user is prompted for confirmation or masking options before the message is sent. | Must |
| F-SALE-3 | Дві критичні PDF-оферти (CS-ITA-01-26, SP_01) відсутні, що є прогалиною у відповідності бізнес-вимогам. | WHEN a user requests a PDF offer for the 'CS-ITA-01-26 (Італія)' or 'SP_01 (Скаутський патруль)' products, THEN the system SHALL generate and provide the correct, complete PDF document for each, matching the defined templates. | Test: Request PDF offers for 'CS-ITA-01-26' and 'SP_01'. Inspect: Verify that the generated PDFs are complete, accurate, and match the specified templates and content for each product. | Must |
| F-SALE-5 | Критерій 'parity з BonSens' нечіткий, відсутні кількісні пороги для верифікації. | WHEN a seat is sold or cancelled, THEN the `event.event.ticket.seats_available` counter MUST be updated within 5 seconds, AND its value MUST match the corresponding seat count in the BonSens system with a deviation of 0. | Test: Automate comparison of system counter vs. BonSens API/DB after seat transactions. | Must |
| F-VAT-1 | Нечітко визначено компонент 'операційка' у Σcost. Потребує конкретизації. | WHILE calculating `Σcost`, THEN the 'операційка' component MUST be calculated as 10% of the sum of (проживання/день + харчування/день + кадра + страхування + канцтовари) per child per day. | Test: Unit test for pricing calculator, verifying 'операційка' value with specific inputs. | Must |
| F-VAT-1 | 'Реклама ≤20% бюджету' є бізнес-обмеженням, а не компонентом формули, його інтеграція в розрахунок неясна. | WHEN the budget is calculated, IF the 'реклама' component of `Σcost` per child exceeds 20% of the `list_price` per child, THEN the system MUST display a warning to the user, AND prevent saving the budget until the 'реклама' component is adjusted to be ≤ 20% of the `list_price`. | Test: Scenario where 'реклама' exceeds 20% threshold, verify warning message and save prevention. | Must |
| F-VAT-2 | Вимога є збіркою бізнес-правил та юридичного контексту, а не атомарною функціональною вимогою. Порушує Singular. | WHEN creating or editing an event's pricing, THEN the system MUST provide a user interface element allowing selection between 'VAT zwolniona' and 'VAT-marża 23%' for the main service, AND the default selection MUST be 'VAT zwolniona'. | Demo: Show UI element for VAT mode selection and confirm default value. | Must |
| F-VAT-2 | Зазначено критичну залежність від 'ЛЮДИНА' та 'interpretacja indywidualna', що вказує на неповне вирішення юридичного ризику. | WHEN a new event is created, THEN the system MUST include a field to record the status of the 'interpretacja indywidualna do KIS' for VAT exemption, with options 'Pending', 'Submitted', 'Approved', 'Rejected', AND the default status MUST be 'Pending'. | Demo: Show the status field in the event creation/edit UI. | Should |
| F-VAT-2 | Верифікація 'vat_mode у wizard' не повністю покриває коректність юридичної інтерпретації та ризиків перекваліфікації. | WHEN a user configures a camp service, THEN the system SHALL provide distinct options for 'zwolniona z VAT' and 'VAT-marża 23%' for the main service. IF 'zwolniona z VAT' is selected, THEN all generated documents (e.g., offers, invoices) SHALL explicitly state 'zwolniona z VAT' and NOT 'маржа 0%'. WHILE merchandise is added to any camp service, THEN the system SHALL automatically apply a 23% VAT rate. | Test: Configure camp with both VAT modes, check document output. Test: Add merchandise, verify 23% VAT application. Inspect: Code for VAT application logic. | Must |
| F-VAT-3 | Повний ланцюг від форми до колонтитулів оферти/умов не доведено, що вказує на неповну верифікацію. | WHEN an organizer's legal form and associated fiscal position are configured, THEN the system SHALL ensure that relevant organizational data (e.g., registration number, VAT status, KRS) is correctly propagated and displayed in the headers and footers of all generated external documents (e.g., offers, invoices, contracts). | Demo: Create an organizer, configure fiscal position, generate an offer/invoice, visually inspect headers/footers for correct data. Test: Automated test for data propagation to QWeb templates. | Must |
| F-FIN-1 | Ключові аспекти вимоги ('аналітика-автостворення', 'кабінет-upload фактур') не верифіковані, що робить вимогу неперевіряємою. | WHEN a new camp event (турнус) is created, THEN the system SHALL automatically create a dedicated `account.analytic.account` linked to that specific camp event. | Test: Create a new camp event and inspect the list of analytic accounts to confirm automatic creation and linkage. | Must |
| F-FIN-1 | Ключові аспекти вимоги ('аналітика-автостворення', 'кабінет-upload фактур') не верифіковані, що робить вимогу неперевіряємою. | WHEN a camp manager accesses their dedicated portal/kiosk, THEN the system SHALL provide a functionality to upload vendor bills (faktury) and link them to the corresponding camp's `account.analytic.account`. | Demo: Log in as a camp manager, navigate to the portal/kiosk, upload a vendor bill, and verify its linkage to the correct analytic account. | Must |
| F-FIN-1 | Вимога об'єднує кілька кроків фінансового процесу (завантаження, прив'язка, звірка), що порушує Singular. | WHEN a camp manager records a vendor bill (faktura), THEN the system SHALL allow the manager to upload the bill document and link it to the specific `account.analytic.account` of the relevant camp. | Demo: Upload a vendor bill, link it to a camp's analytic account, and inspect the bill's details for correct linkage. | Must |
| F-FIN-1 | Вимога об'єднує кілька кроків фінансового процесу (завантаження, прив'язка, звірка), що порушує Singular. | WHEN a vendor bill is linked to a camp's analytic account, THEN the system SHALL allow the manager to categorize the expense using predefined `camp.budget.category` types. | Test: Create a vendor bill, link it to an analytic account, and assign a category from the predefined list, then verify the category is correctly saved. | Must |
| F-FIN-1 | Вимога об'єднує кілька кроків фінансового процесу (завантаження, прив'язка, звірка), що порушує Singular. | WHEN a camp's financial data is reviewed, THEN the system SHALL provide a report or view that compares planned expenses (from `camp.budget`/BEP) against actual expenses (from `account.move` linked to the analytic account) for that camp. | Demo: Generate the camp financial report and visually verify that planned vs. actual expenses are correctly displayed and compared. | Must |
| F-FIN-2 | Requirement combines multiple distinct functionalities (download, auto-send, two formats). Should be split for clarity and independent verification. | WHEN a user views a camp's financial summary, THEN the system SHALL provide a 'Download Evidence' button that allows downloading a report containing income, expenses, balance, and VAT details for that specific camp. | Demo: Navigate to a camp's financial summary, click the 'Download Evidence' button, and confirm a report file is downloaded. | Must |
| F-FIN-2 | Requirement combines multiple distinct functionalities (download, auto-send, two formats). Should be split for clarity and independent verification. | WHEN the 'Download Evidence' button is used, THEN the system SHALL offer the option to download the report in both PDF and XLSX formats. | Test: Click the 'Download Evidence' button, select PDF, verify download. Repeat for XLSX, verify download and content integrity. | Must |
| F-FIN-2 | Requirement combines multiple distinct functionalities (download, auto-send, two formats). Should be split for clarity and independent verification. | WHEN a camp's financial period is closed or upon a scheduled trigger, THEN the system SHALL automatically generate the evidence report (PDF+XLSX) and send it via email to a pre-configured external accountant's email address. | Test: Trigger the auto-send mechanism (e.g., manually or by simulating period close), verify that an email with both PDF and XLSX attachments is received by the configured accountant's email. | Must |
| F-FIN-2 | Content of the report ('доходи/витрати/сальдо/VAT по табору') is not precisely defined, lacking specific accounts, calculation rules, and date ranges. | WHEN the user requests the 'Camp Financial Evidence' report for a specified camp and date range, THEN the system SHALL generate a PDF and XLSX report detailing income, expenses, balance, and VAT for that camp, calculated from `account.move`, `sale.order`, and `payments` linked to the camp's analytic account, including a breakdown by specific general ledger accounts and explicit calculation rules for each section. | Inspect (review report content against defined rules and GL accounts), Test (verify calculations with sample data for accuracy). | Must |
| F-FIN-3 | Critical data integrity issue: 69 sale.order with przychód=0 directly impacts BEP accuracy, making the panel unreliable ('BEP-панель бреше'). | WHILE there are `sale.order` records with `przychód = 0` for events, THEN the system SHALL recalculate `przychód` for these orders to ensure the BEP panel reflects accurate revenue, aiming for 0 `sale.order` records with `przychód = 0`. | Analyze (run a data audit to confirm no `sale.order` has `przychód=0` after fix), Test (verify BEP panel accuracy with corrected data). | Must |
| F-FIN-3 | The requirement is functionally incomplete as the data feeding the BEP calculation is not correctly integrated from sales orders. | WHEN a `sale.order` is confirmed and linked to an event, THEN the system SHALL automatically update the `price_per_child` and `przychód` metrics for the associated event, ensuring these values accurately reflect the revenue from the `sale.order` and correctly feed the BEP calculation. | Test (create a new `sale.order` for an event and verify BEP panel updates correctly), Inspect (review BEP panel for sample events after new sales). | Must |
| F-FIN-4 | The exact calculation rules for 'бізнес-маржа', especially how overhead is applied, are not fully detailed, leading to potential misinterpretation. | WHEN calculating the 'Business Margin' for an event, THEN the system SHALL include all direct costs (e.g., ośrodek, transport, wyżywienie/доба, кадра, ubezpieczenie NNW, atrakcje, gadżety COGS) AND allocated indirect costs (e.g., reklama, general overhead, staff costs not directly tied to VAT margin) based on predefined allocation rules and specific general ledger accounts, providing a detailed breakdown of included costs. | Inspect (review documentation of calculation rules and GL account mappings), Test (verify business margin calculation with sample data and compare to manual calculation). | Must |
| F-FIN-4 | The 'інші' category is an open list, making the definition of cost categories incomplete and potentially ambiguous. | WHILE defining cost categories for margin calculations, THEN the system SHALL ensure that all cost types are explicitly categorized and linked to specific general ledger accounts, eliminating the need for an ambiguous 'інші' (other) category. IF any new cost type arises, THEN it SHALL be assigned to an existing specific category or a new, clearly defined category SHALL be created. | Inspect (review the list of cost categories and their mappings to GL accounts), Analyze (audit existing transactions to ensure all are mapped to specific, non-ambiguous categories). | Should |
| F-OPS-1 | Requirement violates singularity by combining 'diet/allergen aggregation for kitchen' with 'medical/personal item summary for educator'. | WHEN a camp session is active, THEN the system SHALL provide an aggregated summary of all participant diets and EU-14 allergens for kitchen staff, accessible via a dedicated report. | Demo (showing the report generation and content) | Must |
| F-OPS-1 | The 'агрегат-витяг для кухні' is incomplete; its specific format, content, and generation trigger are not fully defined. | WHEN kitchen staff requests a diet/allergen summary, THEN the system SHALL generate a printable report detailing participant names, assigned diet profiles, and declared EU-14 allergens, grouped by camp session. | Inspect (generated report format and content) | Must |
| F-OPS-2 | Requirement violates singularity by describing two distinct transport mechanisms: group bus (camp.transport) and individual escort (camp.escort). | WHEN a participant is assigned to group bus transport, THEN the system SHALL record and display transport details including location, time, vehicle, driver, and associated participant IDs. | Test (verify data storage and display of group transport details) | Must |
| F-OPS-3 | Вимога описує реалізацію (fayna_rule_type, override), а не поведінку системи. Відсутні критерії лояльності для 'banda/platinum/gold'. | WHEN a participant's loyalty status is evaluated, THEN the system SHALL assign them to 'Banda', 'Platinum', or 'Gold' tiers based on predefined, configurable criteria (e.g., number of past participations, total spend). | Inspect (configurable criteria definitions) and Test (tier assignment based on criteria) | Must |
| F-OPS-3 | Відсутні кількісні показники або чіткі критерії для оцінки успішності розширення програми лояльності, що унеможливлює верифікацію. | WHILE the loyalty program is active, THEN the system SHALL track and report on key performance indicators, including the number of participants per loyalty tier and benefit redemption rates, to enable program success evaluation. | Analyze (generated KPI reports) | Should |
| F-OPS-4 | Вимога не є сингулярною, це перелік функцій. Кожну функцію слід виділити в окрему вимогу для чіткості та верифікації. | WHEN a user accesses the personnel training section, THEN the system SHALL display the completion status for mandatory MEN 36h/10h training for each employee. | Inspect (UI for training status). | Must |
| F-OPS-4 | Відсутня деталізація щодо '4 моделі vozhatyi' та їхнього змісту/поведінки, що ускладнює верифікацію та розробку. | WHEN a user accesses the "Школа вожатого" module, THEN the system SHALL provide access to training content and progress tracking for the four explicitly defined "vozhatyi" models, including the ability to generate QWeb certificates upon completion. | Demo (access to module, content, certificate generation). | Must |
| F-OPS-5 | Вимога поєднує функціонал дашборду та механізм аудиту/безпеки (view-as, лог), що може порушувати принцип сингулярності. | WHEN an administrator accesses `/admin/dashboard`, THEN the system SHALL display key performance indicators across 7 distinct sections: business, alerts, active camps, team, communications, marketing+SMS-costs, and audit. | Demo (navigate to dashboard and verify all 7 sections are present and populated). | Must |
| F-OPS-5 | Імплементаційна проблема 'compute_sudo inconsistency' вказує на потенційний ризик для коректності даних або безпеки, що потребує негайного вирішення. | WHILE the system computes data for `camp.marketing.report` and `camp.regulamin`, THEN the system SHALL ensure `compute_sudo` consistency to prevent data corruption and security vulnerabilities, and SHALL not produce any `odoo shell context_get` warnings. | Test (automated tests to verify data consistency and absence of warnings during report generation). | Must |
| F-DOC-2 | Відсутній метод верифікації для основної функціональності (ZIP + об'єднаний PDF). | WHEN a user clicks "Завантажити teczkę", THEN the system SHALL generate and provide for download a ZIP archive containing all specified attachments and a single merged PDF document with a title page and register, ensuring fresh generation of all components. | Demo (click button, download, inspect contents of ZIP and PDF for completeness and correctness). | Must |
| F-DOC-2 | Вимога не містить кількісних порогів для 'свіжа генерація'. | WHEN a user clicks "Завантажити teczkę", THEN the system SHALL generate a ZIP archive and a combined PDF containing all specified documents, ensuring all documents are generated or retrieved within 60 seconds of the request and are not older than 1 minute at the time of generation. | Test: Verify that clicking the button triggers a new generation process and the timestamps on the generated documents are within 60 seconds of the download request. | Must |
| F-DOC-3 | Відсутній метод верифікації. Незрозуміло, як буде підтверджено виконання вимоги після реалізації. | WHEN a user selects documents via checkboxes and clicks "Надіслати інспектору", THEN the system SHALL open a mail composer with the selected attachments and log the action in the camp's chatter. | Demo: Demonstrate the wizard, selection of attachments, opening of the mail composer with attachments, and the log entry in the chatter. | Must |
| F-DOC-3 | Не визначено, які саме 'вибрані attachment' можуть бути надіслані та хто є 'інспектором'. | WHEN a user selects any attachment associated with the camp or child via checkboxes and clicks "Надіслати інспектору", THEN the system SHALL open a mail composer pre-filled with these attachments, addressed to a configurable 'Inspector' email address, and log the action in the camp's chatter. | Inspect: Verify the configuration options for 'Inspector' email and confirm that all relevant attachment types can be selected and sent. | Must |
| F-DOC-4 | Відсутній явний метод верифікації (test/demonstration/inspection/analysis) для автоматичного виконання cron-завдання. | WHILE the system is operational, IF a document's retention period (calculated as `max(date_end останньої реєстрації, signed_date) + retention_years`) has expired and there are no active claims, THEN the system SHALL automatically flag the document for erasure daily via a cron job and log this action. | Test: Create test data with documents exceeding their retention period and verify that the cron job flags them correctly and logs the action. Analyze cron job logs to confirm daily execution. | Must |
| F-I18N-1 | Вимога не виконана, CI червоний через блокери, що перешкоджають мерджу та деплою. | WHEN a user interacts with the language switcher in the kiosk or portal, THEN the system SHALL allow selection between Polish (PL) and Ukrainian (UA), persist this choice on `res.users.lang`, and ensure the selected language is maintained across sessions after re-login. | Test: Verify that selecting a language in both kiosk and portal changes the UI language, that the choice persists after logging out and back in, and that only PL/UA options are available. | Must |
| F-I18N-1 | Необхідно з'ясувати причину помилок `test_art9_http_isolation` та вирішити проблему з .po-дублями. | WHEN PR #15 (Language Switcher) is submitted, THEN the CI pipeline MUST pass with 0 `test_art9_http_isolation` failures, 0 duplicate entries across all `.po` files, and 0 `ruff format` violations. | test | Must |
| F-I18N-3 | Вимога порушує Singular: містить 6+ окремих правил, які слід розділити на окремі вимоги для чіткості та верифікації. | WHEN an Odoo update is deployed using `odoo -u`, THEN existing translations MUST NOT be overwritten unless the `--i18n-overwrite` flag is explicitly used, ensuring 0 unintended overwrites. | inspect | Must |
| F-I18N-3 | Відсутність чітких методів верифікації та вимірюваних порогів для деяких правил (напр., 'звірка людиною', 'post-prod'). | WHEN new or updated translations for key terminology (e.g., 'kierownik', 'wychowawca') are introduced, THEN these MUST be reviewed and approved by at least 1 designated human expert, ensuring 0 unapproved key terminology translations in production. | inspect | Must |
| N-2 | Деякі критерії, такі як 'PDF читається' та 'чат читабельний', є суб'єктивними та потребують кількісних порогів для однозначної верифікації. | WHEN a PDF document is displayed on a mobile device (screen width ≤ 768px), THEN its content MUST be legible without requiring horizontal scrolling or zooming beyond 150%, ensuring 0 instances of illegible content. | test | Must |
| N-3 | Вимога порушує Singular: об'єднує дві різні нефункціональні вимоги (надійність та продуктивність) в одну, що ускладнює управління та тестування. | WHEN a user completes the checkout process, THEN the system MUST respond with confirmation within 500 milliseconds for 95% of transactions under peak load (e.g., 100 concurrent users). | test | Must |
| N-3 | Незважаючи на зафіксовані цілі, відсутність вимірів (бенчмарків) вказує на прогалину в процесі верифікації ([GAP-5]). | WHEN a performance benchmark test is executed, THEN the system shall complete the checkout process within 500ms, submit card data within 500ms, display a list of 10,000 items within 1 second, generate a report within 10 seconds, and complete cron jobs within 60 seconds. | Test (automated performance tests using JMeter/Locust to measure response times). | Must |
| N-4 | Вимога порушує Singular: об'єднує численні, окремі аспекти безпеки (CI, секрети, impersonation, API, публічні форми) в одну вимогу. | WHILE the system handles sensitive configuration parameters, THEN it shall store all secrets outside of version control, utilizing mechanisms like ir.config_parameter or GitHub Secrets, and apply BP-010 filtering when reading configurations. | Inspect (codebase for hardcoded secrets, configuration management system, and CI/CD pipeline for secret injection). | Must |
| N-4 | Відсутність пріоритетів для більшості підпунктів безпеки, крім одного, ускладнює планування та управління ризиками. | WHEN a new software release is prepared, THEN the system shall be reviewed against the OWASP Top 10 checklist, with all identified critical vulnerabilities addressed before deployment. | Inspect (security review reports, penetration test results, and CI/CD scan logs from Bandit/Gitleaks/Safety). | Must |
| N-5 | Вимога описує лише успішний сценарій (бекап, синхронізація), але не визначає поведінку при збої бекапу або невдалій синхронізації. | IF a data backup operation fails, THEN the system shall automatically notify the designated administrator via email within 15 minutes, and all subsequent deployments or migrations shall be blocked until the backup issue is resolved. | Test (simulate backup failure and verify notification delivery and deployment pipeline blocking mechanism). | Must |
| N-5 | Порушення Singular: об'єднує бекапи, lossless-інваріант, staging-нейтралізацію та синхронізацію scratchpad — доцільно розділити на окремі вимоги. | WHEN a database migration or update (`-u`) is performed, THEN the system shall verify the row count for all critical tables to ensure a lossless invariant, with any discrepancy triggering an immediate rollback and alert. | Test (automated pre/post-migration row count comparison scripts for critical tables). | Must |
| T-1 | Вимога не має пріоритету (Must/Should/Could) — необхідно визначити, чи всі 36 тест-файлів обов'язкові для кожного релізу. | WHEN preparing for a release, THEN all 36 unit and integration test files (legal, role-based, domain) must pass. | test, inspect | Must |
| T-2 | Вимога не має пріоритету (Must/Should/Could) — необхідно визначити, чи всі CI-гейти обов'язкові для кожного PR. | WHEN a Pull Request is submitted, THEN all CI gates (Lint, 170 Odoo tests, module-upgrade dry-run) must pass before merging. | inspect | Must |
| T-3 | Вимога описує лише успішний сценарій (зелений з 02.07), але не визначає поведінку при збої E2E-тестів або невдалому виправленні антипатерну. | IF E2E tests (`test_critical_paths.py`) fail on the staging environment, THEN deployment to production must be blocked. | test, inspect | Must |
| T-3 | Вимога не має пріоритету (Must/Should/Could) — необхідно визначити, чи E2E-тести обов'язкові для кожного релізу. | WHEN preparing for a release, THEN all E2E tests (`test_critical_paths.py`) must pass on the staging environment. | test, inspect | Must |
| T-4 | Вимога не має пріоритету (Must/Should/Could) — необхідно визначити, які борги є критичними для релізу, а які можна відкласти. | WHEN preparing for a production release, THEN a UAT with live users must be conducted and signed off according to `docs/TESTING.md`. | inspect | Must |
| M-1 | The statement describes an inventory/baseline, not a functional or non-functional requirement to be implemented. | WHEN preparing for a production release, THEN a quality audit must be generated with a test coverage report showing ≥70%. | analyze | Must |
| M-2 | Violates singularity: combines multiple distinct migration scripts and their characteristics into a single requirement. | The system should have a comprehensive pytest matrix covering RODO role, model, and operation scenarios. | inspect | Should |
| M-2 | Some script descriptions lack specific, measurable outcomes (e.g., 'populate_children_and_events.py', 'populate_step2/3.py'). | The system should include direct tests for Kamilka-escalation cron, SMS cost-guard limit, declaracja-blocking, attachment-ACL PDF, and auto-refusal cron. | inspect | Should |
| M-2 | This is a technical implementation plan for migration scripts, not a high-level functional or non-functional requirement. | The system should have performance benchmarks to ensure optimal operation. | test, analyze | Should |
| M-1 | The statement describes an inventory/baseline, not a functional or non-functional requirement to be implemented. | WHEN the system migration is complete, THEN the target system MUST contain at least 124 child records, 122 consent records, 117 qualification_pdf records, and 107 `sale.signature` records originating from the BonSens system. | Analyze: Execute SQL queries to count records in `child`, `consent`, `qualification_pdf`, and `sale.signature` models and compare with specified thresholds. | Must |
| M-2 | Violates singularity: combines multiple distinct migration scripts and their characteristics into a single requirement. | WHILE any data migration script is executed, THEN it MUST be idempotent, support a DRY_RUN mode, and utilize database savepoints to ensure data integrity. | Inspect: Review script code for idempotency logic, DRY_RUN implementation, and savepoint usage. Test: Execute scripts in DRY_RUN mode and verify rollback functionality. | Must |
| M-2 | Some script descriptions lack specific, measurable outcomes (e.g., 'populate_children_and_events.py', 'populate_step2/3.py'). | WHEN the `populate_children_and_events.py` script is executed, THEN it MUST successfully create or update at least 83 event participation records, linking children to their respective events based on source data. | Analyze: Execute SQL queries to count event participation records in the target system and verify their links to children and events. | Must |
| M-2 | This is a technical implementation plan for migration scripts, not a high-level functional or non-functional requirement. | WHEN the full data migration process is executed, THEN it MUST ensure lossless transfer of all specified BonSens data, achieving 100% data parity for critical entities such as 114 children and 97 signatures. | Analyze: Generate and compare data reconciliation reports for critical entities (e.g., children, signatures) between source and target systems, verifying counts and content. | Must |
| M-2b | Violates singularity: combines two distinct data migration/verification tasks (product↔event links and organizer signature) into one requirement. | WHEN the migration of product-event links is complete, THEN the system MUST ensure that all 59 product-event links from the source system are correctly established in the target system. | Analyze: Execute SQL queries to count and verify product-event links in the target system, comparing against the expected count of 59 from the source. | Must |
| M-2b | The statement is formulated as a task or action item rather than a declarative requirement for the system's state or behavior. | WHEN the legacy system is uninstalled, THEN all 59 `product↔event` links from the legacy system MUST be accurately mapped to the new system, and all `bs_organizer_signature` values MUST match their corresponding source in `портал-karta`. | Inspect: Verify that 59/59 `product↔event` links are mapped and `bs_organizer_signature` values match source via data comparison script. | Must |
| M-3 | The statement is formulated as a task or action item for migration rather than a declarative requirement for the system's state or behavior. | WHEN the legacy system is detached, THEN all RODO consent records from `bs_client_consent` (~122 records) and `rodo_consent.py`/`res_partner_consent.py` MUST be successfully extracted and consolidated into `fayna_rodo_consent_log`. | Inspect: Verify the count of records in `fayna_rodo_consent_log` matches the source count (~122) and content integrity. | Must |
| M-4 | Violates singularity: combines a complex sequence of deployment and migration steps into a single requirement. | WHILE the new portal system is deployed, THEN it MUST operate concurrently with the legacy system, ensuring zero disruption to legacy system functionality. IF the new portal system achieves 100% feature parity with the legacy system, THEN the legacy system MAY be uninstalled. | Test/Analyze: Monitor both systems for concurrent operation without errors. Demo/Test: Conduct UAT to confirm 100% feature parity. | Must |
| M-4 | This is a detailed operational/deployment plan, not a functional or non-functional requirement for the system's behavior or state. | WHILE the new portal system is deployed, THEN it MUST operate concurrently with the legacy system, ensuring zero disruption to legacy system functionality. IF the new portal system achieves 100% feature parity with the legacy system, THEN the legacy system MAY be uninstalled. | Test/Analyze: Monitor both systems for concurrent operation without errors. Demo/Test: Conduct UAT to confirm 100% feature parity. | Must |
| M-4 | Contains internal references ('P1.4', '[F-RODO-7]') that are not defined, hindering external understanding and traceability. | WHILE the new portal system is deployed, THEN it MUST operate concurrently with the legacy system, ensuring zero disruption to legacy system functionality. IF the new portal system achieves 100% feature parity with the legacy system, THEN the legacy system MAY be uninstalled. | Test/Analyze: Monitor both systems for concurrent operation without errors. Demo/Test: Conduct UAT to confirm 100% feature parity. | Must |
| M-5 | Вимога містить множинні критерії, що порушує принцип Singular. Слід розділити на окремі вимоги або підпункти для кожної перевірки. | WHEN the cutover is performed on staging, THEN the row count for each of the 8 specified tables (product, event, ticket, SO, SOL, registration, partner) MUST be identical before and after the cutover. | Inspect (database query comparison of row counts) | Must |
| M-5 | Деякі критерії ('згоди ≈', 'smoke 7 кабінетів', 'migration_needs_review') сформульовані нечітко, потребують конкретизації для однозначної верифікації. | WHEN the cutover is performed on staging, THEN a smoke test MUST be successfully executed for 7 distinct user portals, verifying basic login and functionality. | Test (execute automated smoke test script or manual walkthrough) | Must |
| M-6 | Вимога описує можливості, але не визначає, коли і за яких умов застосовується кожен рівень відкату. Потребує чітких тригерів. | IF a deployment introduces a critical bug solely within the application code that can be isolated, THEN the system MUST support a rollback using an L1 `git revert` operation. | Demo (demonstrate L1 rollback on a test environment) | Must |
| M-6 | Хоча методи відкату описані, відсутність DR-drill означає, що ефективність L4 не верифікована, що є критичним ризиком. | The team MUST conduct a full Disaster Recovery (DR) drill for the L4 rollback procedure at least once every 6 months to verify its effectiveness and recovery time objectives. | Inspect (DR drill report and logs, including RTO metrics) | Must |
| M-7 | Це не вимога, а список відкритих питань. Потребує вирішення та перетворення на конкретні вимоги або рішення. | WHEN migrating product data, THEN duplicate products (general vs. dated) MUST be merged into a single product entry, retaining all relevant historical data. | Inspect (data migration report and sample data verification post-migration) | Must |
| M-7 | Відсутність відповідей на ці питання свідчить про значні прогалини у повноті ТЗ для міграції. | WHEN a migration question (Q2-Q6, Q8-Q10) is identified, THEN it MUST have a documented, agreed-upon resolution. | Inspect migration plan documentation to confirm resolution for all 8 listed questions. | Must |
| M-7 | Немає призначеного власника для вирішення кожного питання, що може призвести до затримок. | WHEN a migration question (Q2-Q6, Q8-Q10) is identified, THEN it MUST have a single, assigned owner responsible for its resolution. | Inspect migration plan documentation to confirm an owner is assigned for each of the 8 listed questions. | Must |
| PR-3 | Вимога містить кілька окремих правил/вказівок, що порушує принцип сингулярності. Кожне правило має бути окремою вимогою. | WHEN deploying code changes, THEN the deployment process MUST strictly follow the sequence: Mac -> GitHub -> staging -> production. | Inspect deployment pipeline configurations and audit logs for adherence to the specified sequence. | Must |
| PR-3 | Фрази 'за «ок»' та 'людина-в-петлі' є неоднозначними без додаткового контексту або опису процесу. | WHEN a `-u`, restart, or deploy operation is initiated, THEN explicit approval from an authorized individual MUST be obtained before execution. | Inspect audit logs and process documentation to verify that all `-u`, restart, and deploy operations have corresponding approval records. | Must |
| PR-3 | Відсутній чіткий метод верифікації (тест/демонстрація/інспекція/аналіз) для підтвердження виконання цих процесуальних правил. | WHILE code is deployed to any server environment (staging or production), THEN direct manual modifications to the code on the server MUST NOT occur. | Analyze server logs for unauthorized file modifications or conduct periodic server inspections to confirm no direct code changes. | Must |
| PR-4 | Вимога містить кілька окремих правил/вказівок для Odoo, що порушує принцип сингулярності. Кожне правило має бути окремою вимогою. | WHEN an Odoo module is upgraded using the '-u' flag, THEN all associated '.pyc' files MUST be cleared before the upgrade process begins. | Inspect CI/CD pipeline configuration and logs to confirm '.pyc' clearing commands are executed prior to Odoo module upgrades. | Must |
| PR-4 | Відсутній чіткий метод верифікації (тест/демонстрація/інспекція/аналіз) для підтвердження дотримання цих правил розробки. | WHEN an Odoo module is upgraded using the '-u' flag, THEN all associated '.pyc' files MUST be cleared before the upgrade process begins. | Inspect CI/CD pipeline logs to confirm `.pyc` clearing commands are executed before Odoo upgrade commands. | Must |
| PR-5 | Вимога є списком критеріїв Definition of Done, що порушує принцип сингулярності. Кожен критерій має бути окремою вимогою або частиною окремого документа DoD. | WHEN a user interacts with any Odoo form, THEN all defined fields MUST be visible, and data entered into them MUST be successfully saved upon submission. | Test (Execute manual or automated UI tests to verify form field visibility and successful data persistence for at least 95% of forms). | Must |
| PR-5 | Деякі критерії (наприклад, 'workflow', 'ux-гейт [N-1]') є неоднозначними або потребують додаткового пояснення для повної ясності. | WHEN a feature is considered ready for release, THEN it MUST pass a UX review by at least one (N-1) designated UX expert, ensuring adherence to established UX guidelines. | Inspect (Review the feature's development ticket or documentation for a formal sign-off by a designated UX expert). | Should |
| GAP-1 | Хоча вимога описує 'Observability-стек', вона перелічує кілька окремих компонентів (метрики, дашборди, алерти, log-retention), що порушує сингулярність. | WHILE the system is in production, THEN all application logs MUST be retained for a minimum period of 30 days. | Inspect (Verify log storage configuration and confirm log availability for the specified 30-day period using log management tools). | Must |
| GAP-2 | Вимога об'єднує дві пов'язані, але окремі функціональності: управління підгрупами та призначення лідерів, що порушує сингулярність. | WHILE an educator manages a camp group of approximately 20 children, THEN the system SHALL allow the educator to divide these children into approximately 3 distinct subgroups. | Test | Must |
| GAP-2 | Фраза 'піраміда +рівень' є неоднозначною. Відсутні деталі щодо функціоналу лідерів та правил їх обрання/призначення. | WHEN a subgroup has been created, THEN the system SHALL allow an educator to designate one participant within that subgroup as its leader, visible via a flag on the participant's profile. | Test | Should |
| GAP-3 | Requirement describes a process, not a system feature, violating 'Conforming' to typical system requirements structure. | BEFORE the system is deployed to production, THEN the project team SHALL conduct a formal User Acceptance Testing (UAT) cycle with at least 3-5 real users (kierownik/parents) on a staging environment, and document the results, including sign-off, in `docs/TESTING.md`. | Inspect | Must |
| GAP-3 | Violates 'Singular' by describing multiple distinct activities (plan, tasks, sessions, sign-off) within one requirement. | WHEN preparing for User Acceptance Testing, THEN the project team SHALL create a usability test plan that includes at least 3-5 specific user tasks (e.g., 'create a camp', 'fill out a card from a phone', 'find a document on control'). | Inspect | Should |
| GAP-3 | Lacks clear, measurable success criteria for the UAT outcome (e.g., user satisfaction, critical bug count), beyond just execution. | AFTER User Acceptance Testing sessions are completed, THEN the project team SHALL obtain formal sign-off from at least 3-5 real users, indicating that no critical blockers prevent system usage and the system meets their core operational needs. | Inspect | Must |
| GAP-4 | Violates 'Singular' by describing two distinct activities (ZAP baseline-scan and external pentest) within one requirement. | WHEN the CI/CD pipeline runs weekly, THEN the system SHALL execute an OWASP ZAP baseline scan against the staging environment URL. | Inspect CI/CD pipeline logs and artifacts. | Must |
| GAP-4 | Lacks clear, measurable success criteria for the security scans (e.g., acceptable vulnerability levels, zero critical findings). | WHEN an OWASP ZAP baseline scan completes, THEN the system SHALL report zero Critical or High severity vulnerabilities. | Inspect OWASP ZAP scan reports. | Must |
| GAP-4 | The condition 'за бюджетом' for external pentest makes the requirement ambiguous and not fully feasible/verifiable. | WHEN preparing for the SaaS phase launch, THEN the product SHALL undergo at least one external penetration test by a certified third-party vendor. | Inspect pentest report and vendor contract. | Must |
| GAP-5 | Lacks explicit performance targets (e.g., specific p95 thresholds) within the requirement itself, relying solely on external reference [N-3]. | WHEN a k6/locust performance test runs on the staging environment for the checkout and 'my account' pages, THEN the system SHALL respond with a p95 latency of less than 500ms. | Inspect CI performance test reports. | Must |
| GAP-6 | Lacks specific success criteria for the DR drill, such as RTO/RPO targets or detailed data integrity checks post-recovery. | WHEN a full disaster recovery drill is performed, THEN the production database and application SHALL be fully restored to a clean environment within 4 hours (RTO) and demonstrate data integrity with less than 1 hour of data loss (RPO). | Inspect DR drill report, runbook, and restored system smoke tests. | Must |
| GAP-7 | Violates 'Singular' by describing two distinct activities (document creation/storage and acknowledgment process) within one requirement. | WHEN the legal requirement for "Standardy Ochrony Małoletніх" (SELLABLE §2.2 п.4) is active, THEN the system SHALL ensure the approved, written policy document is stored in the designated repository (e.g., data/regulamin-type) and accessible to authorized personnel. | Inspect the designated repository for the presence and content of the document. | Must |
| GAP-7 | Describes only the happy path, without considering error handling for document content issues or acknowledgment failures. | WHEN personnel are required to acknowledge "Standardy Ochrony Małoletніх" via the [F-OPS-6] mechanism, IF the acknowledgment process fails (e.g., system error, signature capture failure), THEN the system SHALL notify the user of the specific failure, log the incident, and provide clear instructions for resolution or retry within 5 minutes. | Test by simulating various failure scenarios during the acknowledgment process and verifying error notifications and logging. | Must |
| GAP-8 | Вимога неповна, розпливчаста та не містить вимірних критеріїв щодо того, які дані, коли і як має відбуватися retention/erasure, що робить її неперевіряємою. | WHEN data subject to retention/erasure policies (as defined in [F-DOC-4]) reaches its specified retention period, THEN the system SHALL automatically initiate the erasure process for that data within 24 hours, ensuring data is irrecoverably deleted according to policy guidelines. | Test by setting up test data with specific retention periods and verifying its automatic erasure within the defined timeframe. Analyze system logs for successful erasure. | Must |
| GAP-8 | Вимога не може бути безпосередньо перетворена на критерій приймання у форматі Gherkin (Given/When/Then) через її загальність та відсутність конкретної поведінки | WHEN a user record, marked for deletion after 5 years of inactivity according to [F-DOC-4], reaches its 5-year inactivity threshold, THEN the system SHALL automatically anonymize or delete all personally identifiable information associated with that record within 48 hours. | Test by creating a test user record, simulating 5 years of inactivity, and verifying the data anonymization/deletion within 48 hours. Inspect database for remaining PII. | Must |
| gaps | "Дія без критерію приймання: F-KSK-2 (R4-R9) — немає чіткого DoD для кожного підпункту" | WHEN implementing F-KSK-2 (R4), THEN the system SHALL ensure that all critical system configurations are automatically backed up daily at 02:00 UTC and stored off-site for a minimum of 30 days, verifiable by successful restoration tests performed quarterly. | Test by performing a restoration from an off-site backup for critical system configurations. Inspect backup logs and storage locations. | Must |
| gaps | "Дія без критерію приймання: F-FIN-3 (BEP-живлення) — не визначено, як перевірити коректність" | WHEN процес F-FIN-3 (BEP-живлення) завершується, ТОДІ згенеровані дані ПОВИННІ точно відображати визначені бізнес-правила та вхідні дані з похибкою не більше 0.1%. | test | Must |
| gaps | "Дія без критерію приймання: F-KKW-6 (auto-refusal) — не вказано, як тестувати" | WHEN запит відповідає визначеним критеріям автоматичної відмови F-KKW-6, ТОДІ система ПОВИННА автоматично відмовити у запиті протягом 5 секунд та надати відповідне повідомлення про відмову. | test | Must |
| gaps | "Дія без критерію приймання: F-RODO-2 (attachment-ACL) — не визначено тест на ізоляцію" | WHEN користувач намагається отримати доступ до вкладення, захищеного ACL F-RODO-2, ТОДІ система ПОВИННА надавати доступ лише у випадку, якщо дозволи користувача явно це дозволяють, забезпечуючи повну ізоляцію даних від неавторизованих користувачів. | test | Must |
| gaps | "Дія без критерію приймання: F-KAM-2 (opis порожній) — не вказано, як виправити" | IF поле 'opis' для F-KAM-2 подається порожнім, ТОДІ система ПОВИННА запобігти збереженню запису та відобразити повідомлення про помилку користувачеві, вимагаючи непорожній опис. | test | Must |
| gaps | "Дія без критерію приймання: F-KSK-3 (панель керівника) — не визначено, які compute-поля потрібні" | Панель керівника F-KSK-3 ПОВИННА відображати наступні обчислювані поля: 'Загальний обсяг продажів (сума затверджених рахунків)', 'Середній розмір угоди (загальний обсяг продажів / кількість угод > 0)', 'Очікувані затвердження (кількість елементів у статусі 'Очікування')'. Логіка обчислення кожного поля ПОВИННА бути чітко визначена в системній документації. | inspect | Must |
| gaps | "Дія без критерію приймання: F-FIN-2 (евіденція) — не вказано формат вихідних даних" | WHEN F-FIN-2 generates evidence, THEN the system SHALL output the data in the [SPECIFIED_FORMAT, e.g., CSV or JSON] format, adhering to [VERSION_NUMBER] schema. | Inspect the generated output file to confirm format and schema compliance. | Must |
| gaps | "Дія без критерію приймання: F-COM-4 (DM-чат) — не визначено, як тестувати art.9-гейт" | WHILE a user is composing a message in F-COM-4 (DM-chat), IF the message content triggers the art.9-gate based on [DEFINED_ART9_CRITERIA], THEN the system SHALL prevent message submission and display a warning message: "Message contains sensitive data and cannot be sent." | Test scenarios with messages containing [EXAMPLE_SENSITIVE_DATA] to verify the gate's activation and message blocking. | Must |
| gaps | "Дія без критерію приймання: F-DOC-2 (teczka ZIP) — не вказано, які файли входять" | WHEN F-DOC-2 generates the 'teczka ZIP' archive, THEN it SHALL contain exactly the following files: [LIST_OF_SPECIFIC_FILES_AND_THEIR_STRUCTURE], and no other files. | Inspect the contents of the generated ZIP archive to confirm the presence and absence of specified files. | Must |
| gaps | "Дія без критерію приймання: F-DOC-4 (retention-cron) — не визначено формулу строку" | WHEN F-DOC-4 (retention-cron) executes, THEN the system SHALL apply the data retention formula: "data older than [N] [DAYS/MONTHS/YEARS] from [DATE_CRITERIA, e.g., 'last modification date'] SHALL be archived/deleted." | Analyze system logs and database records after cron execution to confirm data retention policy application. | Must |
| gaps | "Дія без критерію приймання: F-RODO-6 (UODO-export) — не вказано, які поля включити" | WHEN F-RODO-6 generates the UODO-export, THEN it SHALL include all fields mandated by UODO regulations, specifically: [LIST_OF_REQUIRED_FIELDS], and exclude all other personal data fields. | Inspect the generated UODO-export file to confirm the inclusion of required fields and exclusion of non-required sensitive fields. | Must |
| gaps | "Дія без критерію приймання: F-RODO-7 (шифрування) — не визначено, які саме дані шифрувати" | WHEN personal data is processed or stored, THEN the system SHALL encrypt all data categorized as 'sensitive personal data' according to internal data classification policy, using AES-256 encryption. | Inspect data classification policy and system configuration; Test data storage and transmission for encryption presence and strength. | Must |
| gaps | "Дія без критерію приймання: F-REC-3 (документи кадри) — не вказано, які поля обов'язкові" | WHEN a user submits an HR document (F-REC-3), THEN the system SHALL ensure that the following fields are populated: [Employee ID, First Name, Last Name, Date of Birth, Position, Start Date]. | Test form submission with missing mandatory fields; Inspect database schema and UI validation rules. | Must |
| gaps | "Дія без критерію приймання: F-I18N-1 (перемикач мови) — не вказано, як тестувати persistence" | WHEN a user selects a language via the F-I18N-1 switch, THEN this language preference SHALL persist across all subsequent user sessions for that user until explicitly changed or for a minimum of 365 days. | Test by selecting a language, logging out, closing browser, and re-accessing the application to verify persistence. | Must |
| gaps | "Дія без критерію приймання: F-WIZ-7 (high-risk) — не визначено, які активності вважаються high-risk" | The system SHALL identify an activity as 'high-risk' (F-WIZ-7) IF it involves: (1) modification of user roles, (2) financial transactions exceeding 10,000 PLN, or (3) access to more than 100 sensitive records within a 5-minute period. | Inspect system logs and configuration for defined high-risk activity criteria; Test scenarios matching these criteria to confirm correct identification. | Must |
| gaps | "Дія без критерію приймання: F-PRG-4 (Program Wypoczynku PDF) — не вказано, які поля включити" | WHEN generating the 'Program Wypoczynku PDF' (F-PRG-4), THEN the document SHALL include the following fields: [Participant Name, Program Title, Start Date, End Date, Location, Total Cost, Payment Status, Organizer Contact]. | Inspect generated PDF documents for the presence and correct population of all specified fields. | Must |
| gaps | "Дія без критерію приймання: F-WIZ-6 (форми wypoczynku) — не вказано, як перевірити список MEN" | WHEN the system displays the list of F-WIZ-6 forms for 'wypoczynku', THEN it SHALL include all forms officially approved by MEN for the current reporting period. | Inspect (against official MEN documentation for the current period) | Must |
| gaps | "Дія без критерію приймання: F-KKW-7 (картка-еталон) — не визначено, який саме еталон" | WHEN the system processes F-KKW-7 (reference card), THEN it SHALL use the 'Standard Reference Model v2.1' as the definitive reference for data validation. | Inspect (system configuration and documentation for 'Standard Reference Model v2.1') | Must |
| gaps | "Дія без критерію приймання: F-KKW-4 (R1-блокада) — не вказано, які активності блокуються" | IF F-KKW-4 (R1-blockade) is active for a user account, THEN the system SHALL prevent the user from initiating new 'Order Placement', 'Inventory Adjustment', and 'Payment Processing' activities. | Test (attempting blocked activities with an R1-blocked user account) | Must |
| gaps | "Дія без критерію приймання: F-KAM-3 (SLA-протокол) — не визначено, які поля sla_* потрібні" | WHEN the system generates or updates F-KAM-3 (SLA-protocol), THEN it SHALL include the following mandatory fields: `sla_id` (UUID), `sla_status` (ENUM: 'Active', 'Breached', 'Completed'), `sla_start_date` (DATE), and `sla_target_date` (DATE). | Inspect (database schema and API documentation) and Test (attempting to create/update without mandatory fields) | Must |
| gaps | "Дія без критерію приймання: F-OPS-1 (агрегат для кухні) — не вказано, які дані виводити" | WHEN the system displays the F-OPS-1 'Kitchen Operations Aggregate' dashboard, THEN it SHALL present the following data points: 'Daily Meal Count' (integer), 'Ingredient Stock Level' (percentage of capacity), and 'Average Preparation Time' (minutes). | Demo (showing the dashboard with specified data points) | Should |
| gaps | "Дія без критерію приймання: F-MY-4 (фото дня) — не вказано, як тестувати upload з телефону" | WHEN a user uploads a photo from any mobile device (iOS/Android), THEN the system SHALL successfully process and display the photo within 5 seconds. | Test: Upload photos from at least 2 iOS and 2 Android devices, verifying successful display and time taken. | Must |
| gaps | "Дія без критерію приймання: F-MY-5 (preferences) — не вказано, які опції включити" | The system SHALL provide users with a 'Preferences' section that includes options for 'Email Notifications (on/off)', 'Theme (light/dark)', and 'Language (English/Ukrainian)'. | Inspect: Verify UI elements for each preference option and their functionality. | Should |
| gaps | "Дія без критерію приймання: F-COM-2 (cost-guard) — не вказано, як тестувати ліміт" | IF a user's cumulative spending exceeds their configured 'cost-guard' limit (e.g., $100), THEN the system SHALL prevent any further transactions and display an alert message 'Spending limit reached. Please adjust your limit or contact support.'. | Test: Configure a limit, attempt transactions that exceed it, and verify prevention and alert message. | Must |
| gaps | "Дія без критерію приймання: F-COM-3 (auto-канали) — не вказано, які моделі підписуються" | WHEN a new user registers, THEN the system SHALL automatically subscribe them to the 'Welcome Channel', 'Product Updates', and 'Community News' channels. | Test: Register a new user and inspect their subscription list to confirm automatic subscriptions. | Should |
| gaps | "Дія без критерію приймання: F-SALE-2 (генератор картки) — не вказано, як перевірити відповідність канону" | WHEN a product card is generated, THEN it SHALL strictly conform to the 'Product Card Standard v1.2' specification, ensuring fields like 'Product Name (max 50 chars)', 'Price (currency format)', and 'Image URL (valid URL)' are present and correctly formatted. | Inspect: Compare generated product cards against the 'Product Card Standard v1.2' document for all specified fields and formats. | Must |
| gaps | "Дія без критерію приймання: F-SALE-3 (PDF-оферти) — не вказано, як перевірити мапінг" | WHEN a PDF offer is generated for F-SALE-3, THEN all specified data fields from the system SHALL be correctly mapped to their corresponding positions in the PDF document with 100% accuracy. | Test: Generate multiple offers with varying data and visually inspect the mapping accuracy. | Must |
| gaps | "Дія без критерію приймання: F-VAT-3 (форма організатора) — не вказано, як перевірити колонтитули" | WHEN the F-VAT-3 organizer form is generated, THEN it SHALL include the specified header and footer information, correctly formatted and positioned according to the template. | Inspect: Visually verify the presence, formatting, and positioning of headers and footers on generated forms. | Must |
| gaps | "Дія без критерію приймання: F-FIN-1 (факт-витрати) — не вказано, як тестувати upload" | WHEN a user uploads a F-FIN-1 actual expenses file, THEN the system SHALL successfully process the file within 5 seconds and import all valid data records, providing clear error messages for invalid records. | Test: Upload various valid and invalid files, measure processing time, and verify data import completeness and error handling. | Must |
| gaps | "Дія без критерію приймання: F-FIN-4 (дві маржі) — не вказано, які звіти потрібні" | The system SHALL provide reports for F-FIN-4 that clearly display both Margin 1 and Margin 2, calculated according to the defined business logic, and allow filtering by date range and business unit. | Demo/Inspect: Present the reports and verify that Margin 1 and Margin 2 are correctly calculated and displayed based on sample data. | Must |
| gaps | "Дія без критерію приймання: F-OPS-5 (звіти) — не вказано, як тестувати compute_sudo" | WHEN F-OPS-5 reports are generated, THEN the `compute_sudo` function SHALL correctly calculate its value based on the specified input parameters, with an accuracy of at least two decimal places, and display it in the report. | Test/Analyze: Run reports with known input parameters and compare the `compute_sudo` output with pre-calculated expected values. | Must |
| gaps | "Дія без критерію приймання: F-DOC-3 (надіслати інспектору) — не вказано, які документи вибрати" | WHEN a user initiates the "Send to Inspector" action, THEN the system SHALL present a list of all generated compliance documents from the last 24 hours for selection. | Test: Verify that the list contains only compliance documents generated within the last 24 hours and allows selection. | Must |
| gaps | "Дія без критерію приймання: F-I18N-2 (нуль хардкоду) — не вказано, як перевірити grep" | WHILE developing new features, IF any user-facing text is introduced, THEN it SHALL be externalized for internationalization, ensuring zero hardcoded strings in source code files (e.g., .js, .ts, .vue, .html). | Analyze: Perform automated static code analysis (e.g., grep) on relevant source files to confirm no hardcoded strings. | Must |
| gaps | "Дія без критерію приймання: F-I18N-3 (правила процесу) — не вказано, як тестувати --i18n-overwrite" | WHEN the `--i18n-overwrite` flag is used during the build process, THEN the system SHALL update all existing translation keys with new values from the source files, without removing any previously defined keys, and generate a report of overwritten keys. | Test: Run the build with the flag, then inspect generated translation files and the overwrite report to confirm correct updates and no key deletions. | Should |
| gaps | "Дія без критерію приймання: N-2 (mobile-first) — не вказано, які екрани тестувати" | WHEN new UI components or pages are developed, THEN they SHALL be responsive and fully functional on mobile devices (screen widths between 320px and 768px), specifically for the top 5 most frequently visited pages and all new pages introduced in the current sprint. | Test/Demo: Manually test or use automated responsive tests on target mobile device emulators/browsers for the specified pages and screen widths. | Must |
| gaps | "Дія без критерію приймання: N-3 (надійність) — не вказано, як вимірювати продуктивність" | WHILE the system is under typical load (up to 100 concurrent users), THEN the average response time for critical user actions (e.g., login, data submission, report generation) SHALL not exceed 2 seconds, and the system SHALL maintain an uptime of 99.9% over a 30-day period. | Analyze: Conduct performance testing using load simulation tools and monitor system logs/metrics for response times and uptime over the specified period. | Must |
| gaps | "Дія без критерію приймання: N-4 (безпека) — не вказано, як тестувати honeypot" | WHEN a security scan or penetration test is performed on the system, THEN the honeypot must detect and log at least 95% of simulated attack attempts. | Test (simulated attacks and log analysis) | Must |
| gaps | "Дія без критерію приймання: M-2b (BonSens-parity) — не вказано, як перевірити 59/59" | WHEN data migration M-2b is completed, THEN the system must ensure that all 59 critical data attributes for each migrated record match the source system with 100% accuracy. | Inspect (data comparison/audit report) | Must |
| gaps | "Дія без критерію приймання: M-3 (RODO-extract) — не вказано, як перевірити counts" | WHEN a RODO data extraction (M-3) is performed, THEN the system must generate an extract file, and the count of extracted records must exactly match the count of eligible records in the source system. | Inspect (compare source vs. extracted record counts) | Must |
| gaps | "Дія без критерію приймання: M-4 (послідовність прода) — не вказано, як тестувати dry-run" | WHEN the sales sequence (M-4) dry-run is executed, THEN the system must simulate all steps, generate all expected outputs without modifying production data, and complete within 10 minutes. | Test (execute dry-run, verify logs and outputs) | Should |
| gaps | "Дія без критерію приймання: M-5 (критерії приймання cutover) — не вказано, як перевірити row-count" | WHEN the M-5 cutover process is completed, THEN the system must ensure that the total row count for all critical tables in the target system matches the corresponding row count in the source system with 100% accuracy. | Inspect (database row count comparison) | Must |
| gaps | "Дія без критерію приймання: M-6 (rollback) — не вказано, як тестувати DR-drill" | WHEN a Disaster Recovery (DR) drill is performed, THEN the system MUST successfully roll back to the last known good state within 60 minutes, ensuring data integrity is maintained as per RPO/RTO objectives. | Test | Must |
| gaps | "Дія без критерію приймання: GAP-1 (observability) — не вказано, які метрики збирати" | WHILE the system is operational, THEN it MUST collect and expose key performance metrics including CPU utilization, memory usage, network I/O, and request latency (p95, p99) for all critical services, with a minimum resolution of 15 seconds. | Inspect | Must |
| gaps | "Дія без критерію приймання: GAP-2 (звена) — не вказано, які поля моделі" | IF a new 'Order' entity is created, THEN it MUST include the following fields: `orderId` (UUID), `customerId` (UUID), `orderDate` (timestamp), `totalAmount` (decimal, precision 2), and `status` (enum: 'pending', 'processing', 'completed', 'cancelled'). | Inspect | Must |
| gaps | "Дія без критерію приймання: GAP-3 (UAT) — не вказано, які задачі виконувати" | WHEN User Acceptance Testing (UAT) is conducted for the new 'Customer Onboarding' feature, THEN users MUST successfully complete the following tasks: 1) Register a new account, 2) Update profile information, and 3) Initiate a password reset, with 100% success rate for each task. | Test | Must |
| gaps | "Дія без критерію приймання: GAP-4 (security-скан) — не вказано, які інструменти" | WHEN a security scan is performed on the application, THEN it MUST utilize industry-standard tools such as OWASP ZAP for DAST and SonarQube for SAST, configured with critical and high-severity rule sets, to identify vulnerabilities. | Inspect | Must |
| gaps | "Дія без критерію приймання: GAP-5 (performance-бенчмарки) — не вказано, які сценарії" | WHEN performance benchmarks are executed, THEN they SHALL include scenarios covering user login, product search, and checkout processes, achieving a 95th percentile response time under 2 seconds for each. | Test: Execute benchmarks with specified scenarios and verify response times against the threshold. | Must |
| gaps | "Дія без критерію приймання: GAP-6 (DR-drill) — не вказано, як відновлювати" | WHEN a Disaster Recovery (DR) drill is performed, THEN the system SHALL be fully restored from backup within 4 hours, following the documented recovery procedures. | Demo: Conduct a DR drill and demonstrate successful system restoration within the RTO. Inspect: Review recovery documentation. | Must |
| gaps | "Дія без критерію приймання: GAP-7 (Standardy Ochrony) — не вказано, який документ" | WHILE adhering to security standards, THEN the system SHALL comply with all requirements outlined in the 'Internal Security Policy v2.1' document (ID: SEC-POL-001). | Inspect: Review system configurations, access controls, and logs against the specified security policy document. | Must |
| gaps | "Дія без критерію приймання: GAP-8 (retention-механіка) — не вказано, як тестувати cron" | WHEN the data retention cron job is deployed or modified, THEN it SHALL be tested to verify that data older than 365 days is correctly purged without affecting active data. | Test: Execute the cron job in a controlled test environment with sample data, then verify data deletion and integrity of remaining data. | Must |

## Джерела (цитувати у звіті)
- ISO/IEC/IEEE 29148:2018 — Systems and software engineering — Requirements engineering. Шаблон/огляд: https://www.well-architected-guide.com/documents/iso-iec-ieee-29148-template/ ; https://www.cwnp.com/req-eng/
- EARS (Easy Approach to Requirements Syntax), Mavin et al. — шаблони WHEN/WHILE/IF-THEN/WHERE.
- Spec-Driven Development: From Code to Contract in the Age of AI Coding Assistants — https://arxiv.org/html/2602.00180v1
- Spec-Driven Development: The Definitive 2026 Guide — https://thebcms.com/blog/spec-driven-development
- How To Write Clear, Measurable Software Requirements in 2026 — https://www.designrush.com/agency/software-development/trends/software-requirements-specification
- Requirements Smells (Femmer et al.) — https://arxiv.org/pdf/1611.08847
- Well-Formed Quality of System Requirements for ISO 29148-2018 (NLP metric) — https://www.researchgate.net/publication/385802396
- QUARE: Quality-Aware Requirements Analysis through Multi-Agent Dialectical Negotiation — https://arxiv.org/pdf/2603.11890
