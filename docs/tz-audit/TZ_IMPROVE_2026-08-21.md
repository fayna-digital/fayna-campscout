# План покращення ТЗ — /Users/kobzar/Developer/Fayna-Workspace/Projects/fayna_camp_portal/docs/TZ.md

_Дата: 2026-08-21. Кожен рядок: знахідка → готове EARS-формулювання з методом верифікації → пріоритет MoSCoW. Правки застосовує людина/агент окремим кроком._

| Вимога | Проблема | Нове формулювання (EARS) | Верифікація | Пріоритет |
|---|---|---|---|---|
| B-5 | UX-DoD не вимірний: 'самозрозумілий' суб'єктивно; немає методу верифікації. | WHEN користувач відкриває будь-який екран продукту, THEN екран не потребує інструкцій для виконання основного завдання (recognition-not-recall), І текст інтерфейсу відображається польською або українською мовою відповідно до налаштувань користувача. | test: юзабіліті-тест на 5 користувачах цільової аудиторії; 100% виконують завдання без підказок. demo: перемикання мов на staging. | Must |
| B-5 | Двомовність підтверджена, але решта UX-DoD без критеріїв. | WHEN користувач змінює мову інтерфейсу, THEN усі тексти екрана оновлюються на вибрану мову (UA або PL) без перезавантаження сторінки. | demo: перемикання мов на staging; inspect: перевірка, що всі динамічні тексти перекладені. | Must |
| A-5 | Вимога поєднує 4 різні критерії (AGPL, дані, copyleft, межі) — порушення Singular; варто розбити на окремі перевірки. | WHEN виконується збірка продукту, THEN (1) у залежностях відсутні AGPL-модулі, (2) у коді відсутні захардкоджені бізнес-дані (назви таборів у data/ = 0), (3) відсутній copyleft-код, (4) межі модулів є чистими. | inspect: grep-аналіз залежностей, даних та ліцензій; analyze: перевірка архітектури модулів. | Must |
| P-1 | Відсутній метод верифікації: як перевірити, що деплой виконано саме комплектом без завантаження depends з інтернету? | WHEN виконується деплой продукту, THEN всі необхідні модулі (fayna_camp_portal, fayna_rodo_compliance, l10n_pl_ksef_margin, fayna_sms_base, fayna_sms_turbosms, zadarma_odoo та стандартні Odoo) знаходяться в addons_path, І Odoo не завантажує залежності з інтернету. | inspect: перевірка конфігурації addons_path та логів деплою на відсутність мережевих запитів до репозиторіїв модулів. | Must |
| P-2 | Виявлено невідповідність LICENSE у fayna_rodo_compliance та fayna_sms_base — потребує окремого рішення власника. | WHEN модуль fayna_rodo_compliance або fayna_sms_base релізиться, THEN вміст файлу LICENSE у кожному з цих модулів ПОБАЙТНО відповідає тексту ліцензії OPL-1, І нові коміти не містять AI-співавторства. | inspect: hash-порівняння файлів LICENSE з еталонним текстом OPL-1; grep трейлерів AI-співавторства у нових комітах = 0. | Should |
| P-3 | Перелік адаптерів відкритий ('SMSAPI.pl / SerwerSMS.pl') — не визначено мінімальний обсяг для релізу. | WHEN система надсилає SMS, THEN вона має підтримувати щонайменше 2 адаптери: TurboSMS та SMSAPI.pl або SerwerSMS.pl, з роутингом і балансом per адаптер. | test: mock HTTP для кожного адаптера; перевірити вибір адаптера за provider_type. | Could |
| P-3 | Відсутній пріоритет — 'не блокер сезону' не є формальним MoSCoW. | WHEN визначається обсяг релізу, THEN підтримка додаткових SMS-адаптерів має бути класифікована за MoSCoW. | inspect: наявність MoSCoW-пріоритету в документації вимог. | Should |
| F-WIZ-4 | Не вказано метод верифікації нічної тиші (тест/інспекція) та поріг 'вільної години'. | WHEN формується авто-кістяк програми, THEN нічна тиша має тривати щонайменше 9 годин, а вільні години мають бути визначені. | test: test_program_skeleton.py перевіряє тривалість нічної тиші та наявність вільних годин. | Must |
| F-WIZ-6 | Немає пріоритету MoSCoW; вимога змішана з описом реалізації та знахідкою R6.1. | WHEN користувач обирає форму wypoczynku, THEN система має підтримувати повний перелік MEN, включаючи polkolonia та zielona_szkola. | test: test_vacation_forms.py перевіряє наявність усіх форм MEN та синхронізацію між wizard і notification. | Must |
| F-WIZ-6 | Посилання на файли/рядки не є частиною вимоги; ускладнює однозначність. | WHEN користувач обирає форму wypoczynku у wizard, THEN система має зберегти це значення при створенні табору. | test: перевірити, що обране значення vacation_form передається у створений табір. | Must |
| F-WIZ-6 | Згадка 'повний перелік MEN' без конкретного джерела переліку. | WHEN user opens wizard camp_create_wizard.py:85 or camp.kuratorium.notification operations.py, THEN both Selection vacation_form MUST include polkolonia and zielona_szkola, AND action_create_camp MUST persist selected vacation_form to camp record; IF not persisted, THEN system MUST create kuratorium notification wizard with that form. | test: run test_vacation_forms.py; verify full MEN catalog and sync guard; demo: create camp with polkolonia and verify value in camp record and notification wizard | Must |
| F-WIZ-7 | Немає пріоритету; вимога не реалізована, але не позначена як Should/Could. | WHEN event activity type is ski or water, THEN system MUST create instructor vacancy with required license-cert and required insurance attachment, AND event MUST have boolean is_high_risk and insurance fields; IF activity type is not ski/water, THEN no high-risk fields required. | demo: create event with activity type ski; verify instructor vacancy has required license-cert and insurance attachment; inspect event fields is_high_risk and ubezpieczenie | Should |
| F-WIZ-7 | Відсутній метод верифікації; '~1-2 дні' не є критерієм приймання. | WHEN user creates high-risk event (ski or water), THEN system MUST require insurance attachment and licensed instructor before event approval; IF insurance or license missing, THEN approval MUST be blocked. | test: attempt to approve high-risk event without insurance; verify block; demo: add insurance and license, verify approval succeeds | Should |
| F-WIZ-7 | Не визначено, які саме activity-типи тригерять high-risk. | WHEN event activity type is in list [ski, water], THEN system MUST mark event as high-risk and trigger instructor vacancy with license-cert and insurance; IF activity type is not in list, THEN event MUST NOT be high-risk. | inspect: verify activity type list in code; test: create events with ski, water, and other types; verify high-risk flag and vacancy creation only for ski/water | Should |
| F-WIZ-8 | Немає пріоритету; вимога не верифікована, але не позначена як Should/Could. | WHEN user completes photo step in wizard, THEN system MUST save product.image gallery and image_1920, AND website meta/og:image/alt MUST be populated; IF product.image is empty after approve, THEN wizard MUST block approval. | demo: run Playwright photo step and approve; inspect product.image and image_1920; verify website meta tags | Should |
| F-WIZ-8 | SEO-критерії не кількісні (немає порогів для meta/og). | WHEN користувач виконує крок «Фото» у wizard І product.image/image_1920 заповнені, THEN система зберігає згенеровані website meta/og:image/alt з нативним auto-resize/WebP, І кожен згенерований og:image має розмір ≥ 1200×630 px. | test: Playwright-прогін кроку фото; після approve перевірити наявність product.image та значення og:image/alt у HTML картки товару. | Must |
| F-WIZ-8 | Не описано обробку помилок (наприклад, відсутність фото). | IF під час кроку «Фото» у wizard відсутні product.image або image_1920, THEN система показує користувачу повідомлення про помилку І не дозволяє завершити крок до додавання фото. | test: Playwright-прогін кроку фото без зображень; перевірити наявність повідомлення про помилку та блокування завершення кроку. | Must |
| F-PRG-2 | Немає пріоритету; вимога частково реалізована, але не позначена як Should/Could. | WHEN товар має is_rain_plan = true, THEN система рендерить на картці товару як звичайну, так і дощову програму одночасно. | demo: скріншот картки товару після [F-SALE-2], що показує обидві програми. | Should |
| F-PRG-2 | Рендер обох програм на картці не доведений; метод верифікації не конкретизований. | WHEN товар має is_rain_plan = true, THEN система рендерить на картці товару як звичайну, так і дощову програму одночасно. | demo: скріншот картки товару після [F-SALE-2], що показує обидві програми. | Should |
| F-PRG-3 | Вимога сформульована як нотатка задачі з оцінкою (~0.5 дня), а не як системна специфікація поведінки за EARS. | WHEN керівник запускає старт табору І програма (звичайна або запасна) не заповнена на 100% виховником, THEN система блокує запуск табору І показує повідомлення про незаповнену програму. | test: unit-тест на блокування старту табору при незаповненій програмі; demo: індикатор «програма 100%» у панелі керівника. | Should |
| F-PRG-3 | Статус обмеження не підтверджено ('як constraint не підтверджено'), відсутні точні критерії блокування старту. | WHEN старт табору ініціюється, IF програма виховника не заповнена на 100%, THEN система блокує старт та показує індикатор у панелі керівника. | test | Must |
| F-PRG-4 | Текст змішує системну вимогу з беклогом техборгу, оцінкою часу (~1 день) та внутрішніми коментарями. | WHEN користувач відкриває форму програми, THEN система надає можливість друку Program Wypoczynku (Załącznik 9) як PDF через QWeb-звіт. | demo | Should |
| F-REC-1 | Порушено Singular: склеєно флоу вакансій, валідацію §13, історію рефакторингу та майбутній план міграції. | WHEN кандидат подає заявку через публічний /camp/vacancies, THEN система створює запис у camp.staff.vacancy, проходить §13-гейт (KRK/RSPTS) та створює camp.staff у статусі draft. | test | Must |
| F-REC-1 | Вимога містить надлишковий журнальний контекст (дати звірок, плани §8), що ускладнює автоматичний парсинг. | WHEN кандидат подає заявку через публічний /camp/vacancies, THEN система створює запис у camp.staff.vacancy, проходить §13-гейт (KRK/RSPTS) та створює camp.staff у статусі draft. | test | Must |
| F-REC-1a | Порушено Singular: об'єднано 3 різні публічні форми, специфікацію ендпоінта, опис інцидентів та техборг. | WHEN користувач без логіну відкриває /camp/kadry/campscout, /camp/kadry/rodzice або /camp/kadry/wilcza, THEN система надає self-contained HTML+JS форму для генерації PDF та фіксації подання через POST /camp/submit-document. | demo | Should |
| F-REC-1a | Описано тимчасовий стан (стопгап) із планом рефакторингу замість стабільного функціонального контракту. | WHEN користувач без логіну відкриває /camp/kadry/campscout, /camp/kadry/rodzice або /camp/kadry/wilcza, THEN система генерує PDF клієнтською стороною (pdfMake) і фіксує подання через POST /camp/submit-document, зберігаючи PDF, ПІБ, IP і час в ir.attachment. WHEN подання успішне, THEN користувач отримує підтвердження. WHEN подання неможливе (наприклад, збій сервера), THEN система повідомляє про помилку і не втрачає дані форми. | demo: відкрити форму без логіну, заповнити, надіслати, перевірити запис в ir.attachment; test: перевірити обробку помилок POST /camp/submit-document | Must |
| F-REC-2 | Happy_path_only: не специфіковано стан системи у разі відхилення KRK або непроходження перевірки RSPTS. | WHEN працівник (staff=draft) підписує RODO і декларацію RSTPO, THEN він отримує доступ до порталу з розмитими даними дітей. WHEN працівник завантажує KRK і керівник натискає «ДОПУСК», THEN система перевіряє RSPTS через _check_rspts_before_admission. IF перевірка успішна, THEN staff стає active і діти видимі. IF перевірка не пройдена, THEN staff залишається draft, діти залишаються розмитими, і керівник бачить причину відмови. | test: unit-тест RSPTS-гейту (успіх і відмова); demo: e2e-скріншот розмитих дітей до допуску і видимих після | Must |
| F-REC-3 | Вимога описує лише успішний сценарій; не вказано поведінку при відсутності документів або простроченні KRK. | WHEN кадровий документ (KRK, RPS, sanepid, świadectwo) завантажується в ir.attachment на hr.employee або camp.staff, THEN система зберігає його і фіксує дату завантаження. WHEN настає 12 місяців з дати завантаження KRK, THEN система позначає документ як прострочений і блокує допуск працівника до active. WHEN працівник намагається переглянути скани, THEN система перевіряє роль: kierownik бачить свої скани, wychowawca/sales — ні, і кожен перегляд логується в audit log. IF документ відсутній або прострочений, THEN система показує чекліст незаповнених документів у кабінеті staff. | test: unit-тест прострочення KRK (12 міс) і блокування допуску; demo: чекліст документів у кабінеті staff; inspect: audit log після перегляду скана | Must |
| F-REC-3 | Відсутній пріоритет MoSCoW; незрозуміло, чи це Must для релізу. | WHEN працівник відкриває кабінет staff, THEN система відображає чекліст обов'язкових документів (CV, анкета, umowa zlecenia, евіденція годин, рахунок) зі статусом кожного. WHEN працівник завантажує відсутній документ, THEN система зберігає його в ir.attachment і оновлює статус чекліста. WHEN всі документи завантажені, THEN система позначає пакет як повний. IF пакет неповний, THEN система показує список відсутніх документів. | demo: заповнити пакет документів у кабінеті staff і перевірити оновлення чекліста; test: unit-тест статусу чекліста | Should |
| F-REC-4 | Відсутній пріоритет MoSCoW; незрозуміло, чи це Must для релізу. | WHEN адміністратор створює вакансію, THEN система вимагає вказати ставку (wychowawca 220, kierownik 260, instruktor 300 zł/день), опис і вимоги. WHEN працівник скасовує реєстрацію на вакансію, THEN система не змінює статус найнятої кадри автоматично, а позначає потенційний overstaffing прапорцем для ручного рішення. WHEN калькулятор використовує ставки, THEN він бере значення з вакансії. | inspect: поля hr.job містять ставку, опис і вимоги; test: unit-тест скасування реєстрації не змінює статус найнятої кадри; demo: калькулятор використовує ставку з вакансії | Should |
| F-REC-4 | Не вказано метод верифікації для перевірки полів hr.job та скасування реєстрацій. | WHEN вакансія або калькулятор відображає ставку, THEN система показує ставку (wychowawca 220 / kierownik 260 / instruktor 300 zł/день) та поля hr.job (description/requirements). WHEN реєстрацію скасовано, THEN найнята кадра НЕ авто-закривається (прапорець overstaffing, рішення людини). | inspect: перевірити поля hr.job у коді; demo: відкрити вакансію та калькулятор, підтвердити ставки; test: скасувати реєстрацію, перевірити, що кадра не закрита. | Should |
| F-KKW-3 | Відсутній пріоритет MoSCoW; незрозуміло, чи це Must для релізу. | WHEN учасник підписує PDF, THEN система фіксує timestamp, IP, хто, user-agent та зберігає immutable PDF (record-lock write) і append-only журнал `fayna_rodo_consent_log` + HMAC. WHEN зміна після підпису, THEN система створює amendment-copy (нова редакція окремим записом, стара в архів з audit-trail, write-block на оригіналі). | test: `test_rodo_consent_immutable.py`; inspect: grep sha у participant sign-flow; якщо нема — додати поле `signed_pdf_hash` + запис у момент підпису. | Must |
| F-KKW-3 | Механізм 3 (хеш PDF) не підтверджено кодом; вимога неповна без доказу реалізації. | WHEN учасник підписує PDF, THEN система обчислює та зберігає хеш підписаного PDF у полі `signed_pdf_hash` у момент підпису. | inspect: grep sha у participant sign-flow; test: перевірити, що `signed_pdf_hash` записано при підписі. | Must |
| F-KKW-4 | Відсутній пріоритет MoSCoW; вимога не має явного Must/Should/Could. | WHEN учасник має hydrophobia або fear_of_heights, THEN система блокує запис на водні/висотні заняття через ValidationError, БЕЗ override. | test: тест на ValidationError при записі на водні/висотні заняття; якщо constraint відсутній — додати на activity-line registration. | Must |
| F-KKW-6 | Refund — заглушка (_schedule_refund лише логує); реальний механізм повернення не реалізовано. | WHEN за <24 год до старту заїзду реєстрація без підписаної карти АБО без RODO-згоди, THEN система АВТОМАТИЧНО скасовує реєстрацію + повний refund + запис у RODO-лог; існує щоденний cron, що це виконує. | test: тест-фікстура з непідписаною картою → cancel+refund+log; inspect: cron у data/. | Must |
| F-KKW-6 | Вимога описує лише успішний сценарій; відсутня гілка помилки (напр., збій refund). | WHEN реєстрація без підписаної карти АБО без RODO-згоди за <24 год до старту, THEN система автоматично скасовує реєстрацію, виконує повний refund, записує RODO-лог та звільняє місце; WHEN refund не виконано, THEN система логує помилку та повторює спробу. Поріг: 24 год; ідемпотентність: повторний запуск не дублює дії. | test: фікстура з непідписаною картою → cancel+refund+log; test: збій refund → error log+retry; inspect: cron у data/cron.xml | Must |
| F-KKW-7 | Еталон не знайдено/не застосовано; вимога не виконана. | WHEN формується картка учасника, THEN PDF відповідає фірмовому еталону (бренд, лого, реквізити res.company, не «YourCompany»); WHEN еталон відсутній, THEN система використовує MEN-дефолт і логує попередження. | inspect: QWeb-шаблон karta_report використовує дані res.company; demo: PDF містить лого та реквізити; analyze: порівняння з еталоном у репо camp | Should |
| F-KKW-7 | Критерій 'гарніший за MEN-дефолт' суб'єктивний; відсутній кількісний поріг або метод верифікації. | WHEN формується картка учасника, THEN PDF містить щонайменше 3 з 5 фірмових елементів (лого, колірна схема, шрифт, реквізити, контакти), що відрізняються від MEN-дефолту. | inspect: QWeb-шаблон; demo: візуальне порівняння з еталоном; analyze: перевірка наявності елементів у PDF | Should |
| F-KKW-7 | Відсутній пріоритет MoSCoW. | WHEN формується картка учасника, THEN PDF відповідає фірмовому еталону (бренд, лого, реквізити res.company, не «YourCompany»); WHEN еталон відсутній, THEN система використовує MEN-дефолт і логує попередження. | inspect: QWeb-шаблон karta_report використовує дані res.company; demo: PDF містить лого та реквізити; analyze: порівняння з еталоном у репо camp | Should |
| F-RODO-4 | Відсутній формалізований критерій приймання (EARS/Gherkin) та вказаний метод верифікації для майстра видалення. | WHEN користувач запускає wizard видалення на res.partner, THEN система анонімізує дані, логує action='erased' та синхронізує з SendPulse; WHEN дані юридичні (account.move/sale.order) АБО медичні/інцидентні, THEN система не видаляє, а анонімізує; WHEN retention <5 років, THEN система блокує видалення. | test: wizard анонімізує partner та створює лог; test: юридичні дані не видаляються; inspect: наявність wizard у fayna_rodo_compliance | Must |
| F-RODO-4 | Текст містить великий блок аналізу drift з аудиту замість детального опису поведінки wizard та мапінгу полів. | WHEN користувач відкриває wizard Erasure на res.partner, THEN система анонімізує дані, створює лог action='erased' та синхронізує з SendPulse, IF дані юридичні (account.move/sale.order) THEN не видаляє, а анонімізує, IF дані медичні/інцидентні THEN не видаляє. WHEN wizard відсутній, THEN система не надає функціонал erasure. | test: перевірити наявність wizard та його поведінку через UI-тест; inspect: перевірити код на наявність реалізації wizard у fayna_rodo_compliance | Must |
| F-RODO-5 | Відсутні EARS-критерії приймання, метод верифікації та сценарії помилок синхронізації з SendPulse API. | WHEN виконується міграція журналів, THEN система переносить дані з sendpulse_privacy_consent_log у fayna_rodo_consent_log з source='sendpulse'. WHEN продавець переглядає res.partner, THEN система показує стан згоди контакту. WHEN користувач ініціює unsubscribe, THEN система виконує email_opt_out та синхронізує з SendPulse API, IF API недоступний THEN система фіксує помилку та повторює спробу. | test: перевірити міграцію даних та сценарій помилки SendPulse API через unit-тести; demo: продемонструвати стан згоди на res.partner | Should |
| F-RODO-6 | Не визначено метод верифікації та вимірні критерії повноти/структури колонок експортованого звіту. | WHEN організатор натискає кнопку «Завантажити RODO-звіт», THEN система генерує PDF та XLSX звіт, що містить інформацію про згоди, відписки та забуття, фільтровану за partner/camp/датою. WHEN звіт порожній, THEN система показує повідомлення про відсутність даних. | test: перевірити генерацію звіту та його структуру через unit-тести; demo: продемонструвати експорт з фільтрами | Should |
| F-RODO-7 | Порушення singular: вимога об'єднує 5 окремих інфраструктурних вимог (DB шифрування, filestore, бекапи, ротація, аудит-лог). | WHEN система обробляє дані art.9, THEN filestore та Postgres шифруються at-rest (AES-256). WHEN виконується бекап, THEN filestore та DB зберігаються разом офсайт з retention ≥30 днів. WHEN секрети використовуються, THEN вони ротуються ≤90 днів. WHEN audit-лог формується, THEN він є append-only. | inspect: перевірити конфігурацію інфри (LUKS/pgcrypto, бекапи, ротацію) та журнал аудиту | Must |
| F-KAM-1 | Відсутній прямий EARS-критерій та явний метод верифікації (has_verification_method=False, прямий тест у боргу). | WHEN incident має severity='kamilka', THEN система надсилає SMS всім subscribers, навіть при opt-out. WHEN SMS не прочитано протягом 5 хвилин, THEN система ескалює на резервний контакт. WHEN створюється notification log, THEN він є immutable. | test: перевірити ескалацію та логування через unit-тести; demo: продемонструвати сценарій з severity='kamilka' | Should |
| F-KAM-1 | Не визначено поняття 'прочитання' для SMS-повідомлення (delivery receipt vs read receipt) перед ескалацією через 5 хв. | WHEN incident report severity='kamilka' is created AND SMS delivery receipt is not confirmed within 5 minutes, THEN system SHALL escalate via backup contact. WHEN SMS read receipt is confirmed, THEN escalation SHALL NOT occur. | test: automated test simulating SMS without delivery receipt confirms escalation after 300s; test with delivery receipt confirms no escalation. | Must |
| F-KAM-3 | Порушення singular: об'єднано 7 різних часових SLA-інтервалів та вимоги до UI-дашборду (sla_breaches, червоний бейдж). | WHEN incident is created, THEN system SHALL compute sla_breaches for each of 7 time intervals: 0-10s scene safety, 0-2min CPR, 2-15min safe place+ambulance+first parent contact, 15-30min parents fully informed+kierownik on site, 30min-24h organizer+kurator+sanepid/prokurator, 24h-7d witnesses, 7-21d protokół in 3 copies. WHEN any interval exceeds threshold, THEN system SHALL display red badge in dashboard. | inspect: verify sla_* fields exist in emergency.py; test: automated test asserts sla_breaches computed correctly for each interval boundary. | Must |
| F-KAM-3 | Відсутній формалізований метод верифікації та EARS-синтаксис для обчислення computed booleans SLA. | WHEN incident is created, THEN system SHALL compute each SLA boolean per interval threshold. WHEN interval exceeds threshold, THEN system SHALL mark breach in sla_breaches list. | test: automated test with mocked timestamps verifies each computed boolean at boundary values (e.g., 10s, 2min, 15min, 30min, 24h, 7d, 21d). | Should |
| F-KSK-1 | Порушення singular: об'єднано правила перемикання інтерфейсу для різних ролей (керівник, організатор, бекенд). | WHEN user role is kierownik, THEN system SHALL display kiosk tiles with home_action to OWL/QWeb dashboard. WHEN user role is backend, THEN system SHALL hide raw API menus and SHALL show toggle '↔ Odoo'. WHEN user role is organizer, THEN system SHALL show toggle in both directions and kiosk hub with login-as. | demo: manual demo per role verifies UI elements; test: automated test verifies role-based menu visibility and toggle presence. | Must |
| F-KSK-1 | Немає формального методу верифікації (лише 'experiential-прогони') та EARS-критеріїв для перевірки доступу 'login-as'. | WHEN organizer uses login-as, THEN system SHALL grant access to selected user's kiosk view. WHEN organizer uses login-as, THEN system SHALL log the action. WHEN organizer uses login-as, THEN system SHALL restrict access to unauthorized roles. | test: automated test verifies login-as grants correct access and logs action; demo: manual demo verifies access restriction. | Should |
| F-KSK-2 | Вимога містить суміш 7 підзадач (R4-R9, R4b, беклог §18.4) та історію дебагу замість атомарних специфікацій. | WHEN kiosk desktop viewport is active THEN system MUST render mobile-layout, move langbar to header, and show 'Powrót do kiosku' and camp selector. WHEN refund is requested THEN system MUST replace _schedule_refund stub with real refund processing. WHEN PDF attachment is accessed THEN system MUST enforce Attachment-ACL per F-RODO-2. WHEN sale-to-price BEP gap occurs THEN system MUST apply F-FIN-3 logic. WHEN /my/* or kiosk page is accessed THEN system MUST pass mobile-audit N-2. WHEN R7 microcopy is reviewed THEN system MUST clean it and raise coverage from 69% to 70%. WHEN kiosk fix is … | demo | Must |
| F-KSK-2 | Відсутній формалізований синтаксис EARS; змішано стан DoD, історію фіксів та майбутні вимоги. | WHEN kiosk desktop viewport is active THEN system MUST render mobile-layout, move langbar to header, and show 'Powrót do kiosku' and camp selector. WHEN refund is requested THEN system MUST replace _schedule_refund stub with real refund processing. WHEN PDF attachment is accessed THEN system MUST enforce Attachment-ACL per F-RODO-2. WHEN sale-to-price BEP gap occurs THEN system MUST apply F-FIN-3 logic. WHEN /my/* or kiosk page is accessed THEN system MUST pass mobile-audit N-2. WHEN R7 microcopy is reviewed THEN system MUST clean it and raise coverage from 69% to 70%. WHEN kiosk fix is … | demo | Must |
| F-KSK-3 | Об'єднано статус-панель, чек-листи, наскрізні індикатори та генератор нагадувань mail.activity/SMS в один пункт. | WHEN leader views per-camp status panel THEN system MUST show always-visible deadline countdown (21 days krajowy, 14 zagraniczny), state draft→submitted→registered, document checklist (opinia PSP, szkic, KRK/RPS, karty), submission status, and end-to-end indicators (payments/installments, recruitment closed, program 100%, insurance, §13 verification). WHEN deadline approaches THEN system MUST generate reminders via mail.activity. WHEN progress indicator is computed THEN system MUST calculate it via compute fields on event. Verification: demo panel; inspect compute fields and mail.activity … | demo | Should |
| F-KSK-3 | Не описано поведінку та умови для каналу SMS (тригери, таймаути, обробка помилок доставки). | WHEN deadline approaches THEN system MUST generate reminders via mail.activity and SMS. WHEN SMS delivery fails THEN system MUST handle error and retry with timeout. WHEN SMS trigger condition is met THEN system MUST send notification with defined content. Verification: demo SMS trigger and error handling; inspect timeout and retry logic. Priority: Could | demo | Could |
| F-KSK-4 | Вимога дублює F-KSK-3 ('= [F-KSK-3] + hr_recruitment stages') без чітких власних критеріїв приймання. | WHEN leader accesses cabinet THEN system MUST show consolidated process tracker for duties (collect KRK, submit to Kuratorium, sanepid/PSP, fill children, interviews) with reminders, reusing F-KSK-3 and hr_recruitment stages. Verification: demo tracker view; inspect integration with F-KSK-3 and hr_recruitment. Priority: Should | demo | Should |
| F-KSK-4 | Відсутній метод верифікації та вимірні пороги для консолідованого трекер-вигляду. | WHEN керівник відкриває консолідований трекер-вигляд, THEN він бачить статуси всіх обов'язків (KRK, Kuratorium, sanepid/PSP, наповнення дітьми, співбесіди) з нагадуваннями. WHEN статус обов'язку змінюється, THEN трекер оновлюється автоматично. Поріг: 100% обов'язків мають відображатися з актуальним статусом. | demo: відкрити трекер, змінити статус обов'язку, підтвердити оновлення. Перевірити наявність всіх 5 обов'язків. | Must |
| F-KSK-4 | Не визначено точний перелік етапів і логіку агрегації статусів обов'язків. | WHEN керівник переглядає трекер, THEN система показує перелік етапів: зібрати KRK, подати в Kuratorium, sanepid/PSP, наповнення дітьми, співбесіди. WHEN обов'язок має статус 'виконано', THEN він позначається як завершений. Поріг: кожен етап має мати один зі статусів: не розпочато, в процесі, виконано. | inspect: перевірити модель даних статусів та логіку агрегації. Тест: створити обов'язок з різними статусами, перевірити відображення. | Must |
| F-MY-1 | Склейка 9+ маршрутів та специфічної бізнес-валідації згоди (RODO per-child) в одній вимозі. | WHEN батько створює учасника через /my/participants/new, THEN система показує обов'язковий pre-contract consent-блок (RODO art.6(1)(a), per-child). WHEN батько не надає згоду, THEN створення учасника блокується. Поріг: 100% маршрутів /my/* (9+ routes) мають відповідати HTTP 200. | test: виконати test_role_parent_portal.py, test_role_parent_cabinet.py, test_escort_signoff.py, test_portal_camp_day.py. Перевірити, що всі тести проходять. | Must |
| F-MY-2 | Вимога є описом структури меню без специфікації переходів, станів доступності пунктів та рольового доступу. | WHEN батько відкриває головну кабінету, THEN він бачить пункти меню: Karta dziecka, Płatności i faktury, Wiadomości, Dokumenty, Zarezerwuj obóz, Stories, Lojalność. WHEN батько має роль 'parent', THEN всі пункти меню доступні. Поріг: 100% пунктів меню відображаються в PL-версії. | demo: відкрити кабінет батька, перевірити наявність всіх 7 пунктів меню. Перевірити, що кожен пункт веде на відповідну сторінку. | Should |
| F-MY-3 | Не описано стан помилки: що бачить батько, якщо виховник не призначений після рекрутації (збій авто-поділу). | WHEN батько відкриває блок 'Twój wychowawca' і виховник не призначений, THEN система показує порожній блок. WHEN виховник призначений після рекрутації та авто-поділу, THEN блок показує дані виховника. Поріг: 100% випадків, коли виховник призначений, блок має відображати його дані. | demo: створити учасника без виховника, перевірити порожній блок. Призначити виховника, перевірити відображення даних. Скрін кабінету з призначеним виховником. | Should |
| F-MY-4 | Склейка двох функцій: фото дня і погода дня — порушення Singular; розділити на окремі вимоги. | WHEN батько переглядає daily.report для свого заїзду, THEN система показує фото дня з camp.story (published) та погоду дня без службових полів. WHEN вихователь робить фото з телефону (capture=camera), THEN система завантажує його та показує прев'ю. Верифікація: test_story_photo_consent_gate.py для stories; для погоди та upload — demo портального рендеру daily.report та мобільного upload. | demo | Should |
| F-MY-4 | Не описано обробку помилок: що при відсутності фото/погоди, збій upload з телефону. | WHEN фото дня або погода дня відсутні у daily.report, THEN система показує запасний текст-заглушку без помилки. WHEN upload фото з телефону вихователя завершується збоєм, THEN система показує повідомлення про помилку та дозволяє повторну спробу. Верифікація: demo сценаріїв відсутності даних та збою upload. | demo | Should |
| F-MY-5 | Не визначено поведінку при конфлікті: батько вимкнув SMS, але є CRITICAL-подія — чи все одно надсилається? | WHEN батько вимкнув SMS у /my/preferences, THEN система не надсилає SMS навіть для CRITICAL-подій, але надсилає email/push. Верифікація: demo з opt-out та CRITICAL-подією, перевірка відсутності SMS. | demo | Should |
| F-COM-1 | Склейка трьох рівнів пріоритетів і шаблонів в одній вимозі — порушення Singular; розділити на під-вимоги. | WHEN виникає CRITICAL-подія (інцидент, непідписана карта <3 дні, термінова), THEN система надсилає bell+email+push+SMS. WHEN виникає IMPORTANT-подія, THEN система надсилає без SMS. WHEN виникає INFO-подія, THEN система надсилає лише інформаційне повідомлення. WHEN SMS надсилається дитині, THEN потрібен sms_consent батька (RODO art.7) та child_mobile, інакше fallback на батька. Шаблони вожатого: Збір/Підйом/Відбій/НЕГАЙНО-Kamilka, <50 символів, без PII. Верифікація: тест у test-gate для cost-guard (SMS ліміт) та demo шаблонів. | test | Must |
| F-COM-2 | Склейка cost-guard, immutable audit і звіту в одній вимозі — порушення Singular; розділити. | WHEN вожатий надсилає SMS, THEN ліміт 100 SMS/міс, WHEN керівник — 300 SMS/міс (ir.config_parameter), вартість 0.05 zł/сегмент, preview з recipient count. WHEN SMS надіслано, THEN запис у camp.staff.sms.log immutable з retention 7 років. WHEN місяць завершується, THEN дашборд показує місячний звіт. Верифікація: тест на ліміт (додати) та demo логу/звіту. | test | Must |
| F-COM-3 | Немає пріоритету (Must/Should/Could) для вимоги. | WHEN подія створюється/змінюється, THEN auto-створюється приватний discuss-канал «{Camp} — Зміна N» з учасниками kierownik+wychowawcy+інструктори, архів зберігається ≥30 днів після заїзду. | test: створити тестову подію, перевірити склад учасників і архів через 30 днів. | Must |
| F-COM-3 | Матриця auto-subscribe по 8 моделях не наведена — не верифіковано. | WHEN настає подія (карта/інцидент/story), THEN auto-subscribe виконується за матрицею: карта→батько+kierownik+medical; інцидент→повний список; story→батьки заїзду; для всіх 8 моделей поведінка визначена. | test: для кожної з 8 моделей змоделювати подію, перевірити список підписників. | Must |
| F-COM-3 | Немає методу верифікації та кількісних порогів (архів 30 днів — єдине число). | WHEN канал архівується, THEN доступ до нього зберігається ≥30 днів після заїзду; кількість моделей auto-subscribe = 8, кожна має явний список підписників. | test: перевірити архів через 30 днів; inspect: матриця з 8 рядків у коді. | Must |
| F-COM-4 | Немає пріоритету; вимога позначена як не збудована, але без MoSCoW. | WHEN виховник призначений на групу, THEN створюється DM-чат виховник↔батьки групи; WHEN батько надсилає повідомлення з art.9-даними, THEN система показує попередження і маскує поля; весь листування фіксується для RODO-сліду. | demo: створити групу, надіслати art.9-повідомлення, перевірити маскування; inspect: лог листування. | Should |
| F-COM-4 | Немає методу верифікації для RODO-сліду та art.9-гейту. | WHEN DM-чат активний, THEN кожне повідомлення зберігається в журналі; WHEN виявлено art.9-дані, THEN спрацьовує гейт (попередження/маскування) до відправки. | test: надіслати повідомлення з «дайте ліки», перевірити блокування; inspect: журнал містить всі записи. | Should |
| F-COM-4 | Термін '~2-3 дні' не є вимірним критерієм приймання. | WHEN виховник призначає групу в DM-чаті, THEN система створює канал discuss.channel та фіксує листування; WHEN батько вводить дані про ліки, THEN система маскує поля та показує попередження про RODO art.9. Поріг: 100% листування зафіксовано, 100% полів замасковано. | demo: створення чату та введення даних про ліки; inspect: журнал аудиту листування | Must |
| F-SALE-2 | Немає пріоритету (Must/Should/Could). | WHEN система отримує event з structured days, THEN `_generate_product_card(event)` формує картку з daily_routine, activities/highlights, датами/локацією/ціною для програм normal та rain. Поріг: 100% полів заповнено з event/ticket. | test: test_card_generator.py; demo: скрін-гейт відповідності канону Offer 2026 | Must |
| F-SALE-2 | Відповідність канону Offer 2026 не доведена — скрін-гейт не є автоматизованою верифікацією. | WHEN картка товару згенерована, THEN її візуальний вигляд відповідає канону репозиторію `camp/Offer 2026` для обох програм. Поріг: 100% елементів збігаються з каноном. | demo: скрін-гейт при демо; analyze: порівняння з каноном | Should |
| F-SALE-3 | Немає пріоритету; вимога містить незакриті пункти (CS-ITA-01-26, SP_01). | WHEN користувач формує PDF-оферту, THEN система генерує 22 product.document по 12 таборах з COMPAN HEADER, камп-специфічними сторінками, auto-quote та FOOTER. Поріг: 100% таборів мають мапінг, включаючи CS-ITA-01-26 та SP_01. | test: перевірка генерації PDF; inspect: наявність мапінгу для CS-ITA-01-26 та SP_01 | Must |
| F-SALE-3 | Вимога не singular: описує і деплой, і мапінг, і rollback. | WHEN система формує PDF-оферту, THEN вона використовує BP-CAMP-PDF-01..04 з DPI 171 та attached_on='inside' для кожного SKU. Поріг: 100% SKU мають документи з DPI 171. | inspect: перевірка параметрів документів; test: генерація для всіх SKU | Should |
| F-SALE-3 | Немає критерію приймання для повноти мапінгу (всі SKU?). | WHEN генерація PDF-оферти для SKU з каталогу, THEN для кожного SKU з 22 product.document створюється PDF з DPI 171 та attached_on='inside'; WHEN SKU = CS-ITA-01-26 або SP_01, THEN додаються 2 product.document за BP-CAMP-PDF-01..04; WHEN кількість згенерованих PDF < кількість SKU, THEN система блокує генерацію та показує помилку. | test: запустити генерацію для всіх SKU каталогу, перевірити наявність PDF для CS-ITA-01-26 та SP_01; inspect: перевірити DPI та attached_on у згенерованих документах. | Must |
| F-SALE-4 | Відсутній пріоритет MoSCoW для вимоги. | WHEN користувач відкриває меню розстрочки, THEN система показує модуль fayna.payment.installment.*; WHEN виконується пошук у commercial.py/camp.py, THEN знаходяться згадки installment; WHEN перевіряється наявність у БД, THEN installment існує як per-order state machine. | demo: продемонструвати роботу розстрочки в меню; inspect: grep commercial.py/camp.py та перевірка наявності записів у БД. | Should |
| F-SALE-4 | Критерій приймання не сформульовано; лише доказ існування коду. | WHEN користувач створює замовлення з розстрочкою, THEN система створює запис у fayna.payment.installment.*; WHEN замовлення оплачено повністю, THEN стан installment змінюється на 'paid'; WHEN оплата прострочена, THEN стан змінюється на 'overdue'. | test: створити тестове замовлення з розстрочкою, перевірити створення запису та зміну станів; inspect: перевірити стан у БД після оплати. | Should |
| F-SALE-6 | Відсутній пріоритет MoSCoW. | WHEN користувач відкриває /camp/zwrot або /camp/oferta/ferie2027 без логіну, THEN система показує self-contained HTML з static/src/kadry/; WHEN користувач заповнює форму zwrot, THEN генерується client-side PDF та надсилається на сервер, Telegram та email. | demo: відкрити сторінки без логіну, заповнити форму zwrot, перевірити отримання PDF на сервері/Telegram/email; inspect: перевірити controllers/kadry_forms.py. | Should |
| F-SALE-6 | Не описано обробку помилок (збій PDF, недоставка email/Telegram). | WHEN генерація PDF у /camp/zwrot завершується помилкою, THEN система показує користувачу повідомлення про помилку та не надсилає дані; WHEN відправка на Telegram або email не вдається, THEN система повторює спробу до 3 разів; WHEN повторні спроби невдалі, THEN система логує помилку та повідомляє адміністратора. | test: імітувати збій PDF та недоставку Telegram/email, перевірити поведінку системи; inspect: перевірити лог помилок. | Should |
| F-VAT-1 | Відсутній пріоритет MoSCoW. | WHEN користувач створює бюджет у майстрі, THEN калькулятор обчислює list_price = (Σcost ÷ діти) × (1+markup) × (1+VAT), де Σcost включає проживання/день, харчування/день, кадру (260/300/220 zł/день ÷ діти), страхування, мерч (VAT 23%), канцтовари, операційку, рекламу ≤20% бюджету; markup за замовчуванням 5%, редагується, ≥5%. | test: test_pricing_calculator.py, test_budget.py; demo: action_apply_computed_price створює budget з обчисленою ціною | Must |
| F-VAT-2 | Відсутній пріоритет MoSCoW. | WHEN користувач налаштовує VAT-режим у wizard, THEN доступний перемикач між 'zwolniona z VAT' (за замовчуванням, art.43 ust.1 pkt 24 lit.a, умови: форма oświaty ≥2 дн, zgłoszenie kuratorium ✓ 25366/WIE/L-2026, реальний nadzór+програма) та 'VAT-marża 23%' (art.119); мерч завжди 23%; кадра zlecenie поза VAT; підпис 'zwolniona', не 'маржа 0%'; 'дитячий одяг 5%' виключено. | inspect: vat_mode у wizard; analyze: research 24.06, джерела KIS; demo: перемикач відображається та змінює режим | Must |
| F-VAT-2 | Критерій приймання не визначений; лише доказ наявності vat_mode. | WHEN користувач обирає VAT-режим 'zwolniona' у wizard, THEN система застосовує ставку 0% VAT до послуги табору; WHEN обирає 'VAT-marża 23%', THEN застосовує 23%; мерч завжди 23%; кадра zlecenie поза VAT; підпис 'zwolniona', не 'маржа 0%'. | test: перевірити, що зміна vat_mode змінює обчислену ціну відповідно до обраного режиму; demo: створити бюджет з кожним режимом і перевірити підпис | Must |
| F-VAT-3 | Не вказано пріоритет (MoSCoW) для вимоги. | WHEN користувач створює організатора, THEN обирає форму: фірма (rejestr turystyki: № wpisu; VAT-marża; zaliczki) або fundacja/NGO (KRS; без marży; може безкоштовно); дані форми відображаються на оферті, фактурах, договорі (колонтитули). | inspect: fiscal_position у wizard (grep); demo: створити організатора обох форм і перевірити колонтитули оферти/фактури/договору | Should |
| F-VAT-3 | Відсутній кількісний критерій для 'SaaS-ready' — не вимірюється. | WHEN користувач створює організатора, THEN система підтримує щонайменше дві форми: фірма (з полями rejestr turystyki, № wpisu, VAT-marża, zaliczki) та fundacja/NGO (з полем KRS, без marży, можливість безкоштовно); WHEN створюється оферта/фактура/договір, THEN дані форми організатора відображаються у колонтитулах. | test: створити організатора кожної форми, згенерувати документи, перевірити наявність даних у колонтитулах; demo: скріншоти документів для обох форм | Should |
| F-FIN-1 | Не вказано пріоритет (MoSCoW). | WHEN керівник завантажує vendor bill з прив'язкою до account.analytic.account табору, THEN система звіряє план (camp.budget/BEP) з фактом (account) і відображає результат у P&L; WHEN event створюється, THEN аналітика створюється автоматично; WHEN керівник завантажує фактуру через кіоск, THEN вона прив'язується до аналітики табору. | test: перевірити автостворення analytic на event-create; demo: завантажити фактуру через кіоск і підтвердити прив'язку; inspect: наявність upload-роуту. | Must |
| F-FIN-1 | Не описано обробку помилок: що при некоректній прив'язці фактури до аналітики. | WHEN vendor bill завантажено без коректної прив'язки до account.analytic.account табору, THEN система показує попередження і не приймає фактуру до звірки; WHEN прив'язка валідна, THEN фактура включається у P&L. | test: створити фактуру без аналітики, підтвердити попередження; demo: завантажити некоректну фактуру, перевірити відхилення. | Must |
| F-FIN-2 | Не вказано пріоритет (MoSCoW). | WHEN керівник натискає кнопку «Скачати евіденцію», THEN система генерує PDF і XLSX з доходами, витратами, сальдо і VAT по табору; WHEN активовано opt-in місячний cron, THEN система автоматично надсилає PDF на email бухгалтера; WHEN надсилання успішне, THEN у chatter логується запис. | test: перевірити генерацію PDF і XLSX; demo: натиснути кнопку, підтвердити файли; inspect: наявність кнопки на панелі [F-KSK-3]. | Should |
| F-FIN-2 | Не описано сценарій збою: що при невдалій відправці на email. | WHEN надсилання PDF на email бухгалтера не вдається, THEN система логує помилку у chatter і показує повідомлення керівнику; WHEN повторна спроба можлива, THEN керівник може ініціювати надсилання вручну. | test: імітувати збій SMTP, підтвердити лог і повідомлення; demo: перевірити ручну повторну відправку. | Should |
| F-FIN-3 | Не вказано пріоритет (MoSCoW). | WHEN sale.order підтверджено і прив'язано до event, THEN przychód і price_per_child події оновлюються автоматично; WHEN подія нижче BEP і власник намагається опублікувати або активувати, THEN система показує попередження, але не блокує; WHEN sale.order має przychód=0 при підтверджених продажах, THEN таких записів 0. | analyze: SQL-аудит, перевірити 69→0 записів з przychód=0; test: створити новий SO, підтвердити оновлення BEP-панелі; test: спроба публікації нижче BEP, підтвердити попередження. | Must |
| F-FIN-3 | Розрив: 69 sale.order з przychód=0 — продажі не живлять BEP-панель. | WHEN sale.order підтверджено і прив'язано до event, THEN przychód і price_per_child події оновлюються автоматично; WHEN przychód=0 при підтверджених продажах, THEN кількість таких записів = 0. | analyze: SQL-аудит sale.order з przychód=0 (69→0); test: новий SO → BEP-панель оновлюється. | Must |
| F-FIN-4 | Не вказано пріоритет (MoSCoW). | WHEN формується Zestawienie obozów, THEN звіт містить przychód, koszty, VAT-маржу та бізнес-маржу для кожного obozu. | demo: відкрити звіт report_season_budgets для сезону з даними; inspect: наявність обох марж у шаблоні. | Should |
| F-FIN-4 | Rejestr faktur za miesiąc — TODO, не реалізовано. | WHEN користувач обирає місяць, THEN Rejestr faktur показує numer, kontrahent, obóz, data zapłaty, kwota marża, kwota gadżety для всіх faktur за місяць. | demo: згенерувати Rejestr faktur за місяць з тестовими даними; inspect: наявність усіх полів. | Should |
| F-FIN-5 | Не вказано метод верифікації (test/demonstration) та кількісні пороги для 'повне повернення коштів'. | WHEN настає дедлайн рішення і заїзд нижче BEP, THEN організатор одним wizard-ом скасовує заїзд: cancel реєстрацій, авто-повідомлення батькам (SMS CRITICAL + email), повне повернення коштів (100% суми), звільнення місць, лог рішення. | test: створення заїзду нижче BEP, виконання wizard, перевірка cancel, refund=100%, notify, лог; demo: ручний сценарій. | Must |
| F-FIN-5 | Відсутній опис edge cases: часткові оплати, комісії платіжних систем, конкурентні скасування. | WHEN скасування заїзду ініційовано, THEN система обробляє часткові оплати (повертає фактично сплачене), комісії платіжних систем (не повертаються), конкурентні скасування (блокує повторні дії). | test: сценарії з частковою оплатою, комісією, двома одночасними скасуваннями; analyze: перевірка логіки refund. | Should |
| F-VAT-4 | Пріоритет не зазначено в тексті вимоги (лише 'money-critical'). | WHEN фактура заїзду виставляється з vat_mode=marża, THEN податкова база = маржа (ціна − koszty з прапорцем «до VAT-маржі» з camp.budget/analytic), фактура несе позначку «Procedura marży dla biur podróży» БЕЗ ставки/суми VAT на рядку (art.119), а KSeF-експорт FA(3) заповнює P_PMarzy через `l10n_pl_ksef_margin`. | test: створити заїзд з vat_mode=marża, виставити фактуру, перевірити податкову базу, позначку та KSeF-експорт (P_PMarzy). | Must |
| F-OPS-1 | Агрегат для кухні не має критерію приймання (що саме виводиться, у якому форматі). | WHEN користувач кухні відкриває агрегат дієт/алергій по табору, THEN виводиться зведення «2 ліки, 4 окуляри...» для виховника, згруповане за дієт-профілями та алергенами. | demo: відкрити агрегат для табору з тестовими даними, підтвердити наявність зведення у зазначеному форматі. | Should |
| F-OPS-4 | Відсутній пріоритет MoSCoW для вимоги та підзадач (портал у backlog). | WHEN користувач відкриває портал /my/training, THEN він бачить свої тренінги, сертифікати та статус прострочення. | demo: авторизуватись як співробітник, відкрити /my/training, перевірити відображення даних. | Could |
| F-OPS-4 | Вимога описує лише успішний сценарій; не вказано обробку помилок (напр., невалідна дата тренінгу). | WHEN користувач вводить невалідну дату тренінгу (наприклад, у минулому або пізніше ніж 10 років), THEN система показує зрозуміле повідомлення про помилку та блокує збереження. | test: спробувати зберегти тренінг з невалідною датою, перевірити повідомлення та відсутність запису. | Should |
| F-DOC-1 | Відсутній пріоритет; вимога описує архітектурний стан, а не поведінку. | WHEN користувач шукає документ у реєстрі, THEN він знаходить його за типом (продаж/фінанси, правне, кадри, RODO) та місцем зберігання (native або custom+attachment). | inspect: перевірити наявність реєстру та можливість пошуку за типами документів. | Must |
| F-DOC-1 | Немає методу верифікації для '~90% документів = native' — поріг невимірний. | WHEN перевіряють реєстр документів, THEN щонайменше 90% типів документів (оферта, umowa, фактури, рати, поліса, zgłoszenie, karta, program, dziennik, opinie, кадри, RODO) мають бути зіставлені з native-моделями або custom+attachment, IF-THEN частка native-документів ≥90% від загальної кількості типів. | analyze: підрахунок типів документів у реєстрі та зіставлення з моделями; частка ≥90%. | Must |
| F-DOC-2 | Відсутній пріоритет; частина функціоналу (ZIP-збірка) не реалізована. | WHEN користувач натискає «Завантажити teczkę», THEN система формує ZIP-архів та об'єднаний PDF з титулкою-реєстром, що містить усі перелічені складники (оферта, договір, фактури, karty, program, dziennik, zgłoszenie, KRK, świadectwa, PSP, sanepid, szkic, поліса, RODO-zgody), IF-THEN генерація завершується без помилок протягом 2 хвилин. | demo: натискання кнопки, перевірка наявності ZIP та PDF з усіма складниками; test: автоматизований тест генерації. | Should |
| F-DOC-2 | Не описані крайні випадки: відсутність обов'язкових документів, помилки генерації PDF. | WHEN у teczці відсутні обов'язкові документи АБО виникає помилка генерації PDF, THEN система показує користувачу список відсутніх документів та повідомлення про помилку, IF-THEN процес зупиняється без створення неповного архіву. | test: unit-тест з відсутніми документами та з помилкою PDF; demo: ручна перевірка сценарію. | Should |
| F-DOC-3 | Відсутній пріоритет (MoSCoW) для вимоги. | WHEN користувач обирає документи галочками та натискає «Надіслати інспектору», THEN система створює повідомлення mail.compose.message з вибраними attachment та логує дію у chatter табору. | demo: вибір документів, надсилання, перевірка повідомлення та логу в chatter. | Could |
| F-DOC-3 | Не вказано метод верифікації (test/demonstration). | WHEN користувач обирає документи галочками та натискає «Надіслати інспектору», THEN система створює повідомлення mail.compose.message з вибраними attachment та логує дію у chatter табору. | test: автоматизований тест створення compose-повідомлення та логування. | Could |
| N-1 | Відсутній пріоритет (Must/Should/Could) для вимоги. | WHEN будь-який екран будь-якої ролі претендує на статус «готовий», THEN він має пройти ux-nielsen-reviewer (скріншот-аудит) і не мати FAIL за евристиками 3, 6, 8, 9; новий екран — спершу /ux-design. WHEN змінюється існуючий екран, THEN повторний прогін обов'язковий. Порушення R4/R4b/R7-R9 ([F-KSK-2]) мають бути усунені до готовності. | demo: прогін ux-nielsen-reviewer на кожному екрані перед готовністю, відсутність FAIL за критичними евристиками | Must |
| N-1 | Критерій 'критичні (3/6/8/9)' не визначений кількісно; метод верифікації не специфікований. | WHEN ux-nielsen-reviewer виконує аудит екрана, THEN критерій FAIL за евристиками 3, 6, 8, 9 визначається як наявність хоча б одного порушення, що відповідає опису цих евристик у чеклісті; кількісний поріг — 0 порушень для статусу «готово». | inspect: чекліст з описом евристик 3, 6, 8, 9; demo: аудит екрана з відомим порушенням дає FAIL | Must |
| N-2 | Відсутній пріоритет (Must/Should/Could) для вимоги. | WHEN будь-який екран /my/* або кіоск змінюється, THEN він має пройти mobile-audit на реальному телефоні: touch-цілі ≥44px, контраст WCAG AA, PDF читабельний, фото-upload з камери працює, чат читабельний; responsive-grid недостатньо. WHEN перед cutover, THEN систематичний прогін по всіх екранах обов'язковий. | demo: mobile-audit на реальному телефоні для кожного зміненого екрана; inspect: звіт аудиту перед cutover | Must |
| N-3 | Відсутній пріоритет (Must/Should/Could) для вимоги. | WHEN виконується checkout або submit карти, THEN час відповіді має бути <500ms; WHEN завантажується список 10k записів, THEN <1s; WHEN формується звіт, THEN <10s; WHEN виконується cron, THEN <60s. WHEN збій оболонки, THEN продажі/оплати не блокуються (graceful degradation). | test: бенчмарки для кожного сценарію; demo: імітація збою оболонки, продажі/оплати працюють | Must |
| N-4 | Відсутній пріоритет (Must/Should/Could) для вимоги. | WHEN випускається реліз, THEN OWASP Top 10 чекліст виконано; CI запускає bandit/gitleaks/safety (ruff S*); секрети не зберігаються в git (ir.config_parameter, GitHub Secrets; BP-010 фільтр при читанні конфігів). WHEN виконується імперсонація, THEN використовується with_user + HMAC-патерн BP-004 з audit-логом, не sudo. WHEN публічна форма заявок активна, THEN honeypot + rate-limit + mime-whitelist реалізовано. | inspect: CI-лог з bandit/gitleaks/safety; demo: спроба IDOR через /api/v1 відхилена; inspect: конфіг публічної форми | Must |
| N-5 | Відсутній пріоритет (Must/Should/Could) для вимоги. | WHEN performing backup before each `-u`/migration, THEN backup must be created and row-count invariant must hold (lossless), AND staging must be neutralized (mail/SMS/crons off) during refresh from prod, AND scratchpad-stand must be synced to git before `-u` (lesson R1). | inspect backup logs and row-count reports; demo staging neutralization; inspect git sync status | Must |
| T-1 | Вимога не має пріоритету (MoSCoW) — незрозуміло, чи є Must для релізу. | WHEN running unit/integration tests, THEN the full suite (43 test files as of 2026-07-06, source of truth `ls tests/test_*.py`) must pass, covering legal, role, domain, and reuse-migration groups. | test: execute `pytest tests/` and verify all pass; inspect file count via `ls tests/test_*.py` | Must |
| T-2 | Вимога не має пріоритету (MoSCoW) — незрозуміло, чи є Must для релізу. | WHEN a PR is submitted, THEN CI gates (lint: ruff, ruff-format, bandit, gitleaks, OCA checks, checks-po; test-gate: full module suite; module-upgrade dry-run) must pass, AND branch protection must require review approval and up-to-date branch. | inspect CI run status on PR; demo green run on main; inspect branch protection settings | Must |
| T-4 | Вимога не має пріоритету (MoSCoW) — незрозуміло, чи є Must для релізу. | WHEN coverage is measured, THEN it must be ≥70% (currently 69% as of 2026-07-04, delta ~90 stmts), AND known test debts (RODO matrix, direct tests for Kamilka escalation, SMS cost-guard, declaracja-blocking, attachment-ACL PDF, performance benchmarks, UAT with live users) must be addressed before prod. | analyze coverage report; test: run pytest for direct tests; demo UAT sign-off procedure | Should |
| M-2b | Не вказано, хто відповідальний за дописування мапінгу та контрольну звірку. | WHEN completing BonSens-parity before uninstall, THEN product↔event links must be migrated 42/59 (remaining to be mapped), AND bs_organizer_signature must be verified against portal-karta source, AND mapping must be completed in detach/populate with final verification 59/59, before uninstall window. | inspect mapping completion in detach/populate; demo verification 59/59; analyze signature source | Should |
| M-4 | Критерій (а) 'без жодного збою' не вимірюється; відсутній поріг допустимих збоїв. | WHILE портал розгорнутий на проді, THEN legacy-функції працюють без збоїв; допустимий рівень збоїв = 0 інцидентів за 24 год після деплою. | analyze: моніторинг обох систем фіксує 0 інцидентів за 24 год; demo/UAT підтверджує feature-parity. | Must |
| M-5 | Вимога поєднує кілька незалежних перевірок (row-count, PDF, підписи, smoke), що ускладнює трасування до окремих тестів. | WHILE виконується cutover, THEN кожна перевірка має окремий критерій: row-count 8 таблиць ДО=ПІСЛЯ; PDF 81=81; підписи 107=107; smoke 7 кабінетів = 7/7 pass. | inspect: SQL-порівняння row-count; test: smoke-скрипт для 7 кабінетів; inspect: PDF/підписи. | Must |
| M-6 | Відсутній метод верифікації для рівнів rollback; не визначено, як підтвердити успішність кожного рівня. | WHILE виконується rollback L1-L4, THEN кожен рівень має підтверджений успішний результат; DR-drill має бути проведений до деплою. | test: DR-drill виконується і фіксує успішне відновлення; analyze: журнал rollback. | Must |
| M-6 | Не вказано пріоритет (Must/Should) для виконання DR-drill; GAP-6 не має терміну усунення. | WHILE GAP-6 не усунено, THEN деплой на прод заборонено; DR-drill має бути виконаний і задокументований до наступного релізу. | inspect: звіт DR-drill з датою і результатом; analyze: відповідність графіку. | Must |
| M-6 | Вимога описує процедуру, але не містить критеріїв приймання для кожного рівня відкату. | WHILE виконується rollback L1-L4, THEN кожен рівень має критерій приймання: L1 код відновлено, L2 manifest відновлено, L3 schema відновлено ≤15 хв, L4 повне відновлення. | test: перевірка кожного рівня на staging; inspect: час виконання L3. | Must |
| M-7 | Вимога містить 8 відкритих питань, кожне потребує окремого рішення; порушує Singular. | WHEN виконується міграція, THEN кожне відкрите питання (Q2–Q10) має одне зафіксоване рішення з власником і датою; IF рішення не прийняте до початку міграції, THEN міграція не стартує. | inspect: реєстр рішень містить по одному запису на Q2–Q10 з власником і датою; demo: відсутність незакритих питань у списку. | Must |
| M-7 | Жодне питання не має критеріїв приймання або методу верифікації; неможливо перевірити виконання. | WHEN кожне питання Q2–Q10 вирішується, THEN рішення має критерій приймання та метод верифікації (test/demo/inspect/analyze); IF критерій відсутній, THEN рішення не вважається прийнятим. | inspect: кожен запис рішення містить критерій і метод; demo: перевірка виконання критерію на стенді. | Must |
| M-7 | Не вказано пріоритетів для вирішення питань; терміни відсутні. | WHEN формується план міграції, THEN кожне питання Q2–Q10 має пріоритет (MoSCoW) і граничну дату; IF дата пропущена, THEN питання ескалюється власнику. | inspect: план містить пріоритет і дату для кожного питання; analyze: журнал ескалацій для прострочених дат. | Should |
| PR-1 | Вимога описує процес, але не визначає конкретних критеріїв для кожного гейта (наприклад, що саме перевіряє Odoo-reviewer). | WHEN PR проходить гейт Odoo-reviewer, THEN reviewer перевіряє відповідність коду ADR і відсутність відомих антипатернів; IF перевірка не пройдена, THEN PR відхиляється з конкретним коментарем. | test: набір чек-листів гейта виконується на PR; demo: відхилений PR містить коментар із причиною. | Must |
| PR-2 | Вимога поєднує три незалежні правила (доказ, login-as, adversarial judge); доцільно розділити. | WHEN виконавець заявляє «готово», THEN у тому ж ході надається fresh-доказ (exit code/вивід/рядок) та login-as кожною роллю без помилок; IF доказ відсутній, THEN заявка не приймається. | test: автоматична перевірка наявності доказу; demo: login-as для кожної ролі завершується без помилок. | Must |
| PR-3 | Не визначено пріоритет (MoSCoW) для вимоги. | WHEN виконую deploy, THEN дотримуюсь послідовності Mac → GitHub → staging → prod, без правок на серверах; WHEN виконую -u/restart/deploy, THEN отримую «ок» від відповідальної особи; WHEN передаю секрети, THEN використовую heredoc згідно BP-010; WHEN виконую deploy, фінал RODO, бізнес-рішення, слова/тон, ціна/VAT, THEN залучаю людину-в-петлі. | inspect: перевірити наявність записів у журналі deploy про дотримання послідовності та підтверджень «ок»; demo: продемонструвати процес передачі секретів через heredoc. | Must |
| PR-3 | Відсутній метод верифікації та кількісні критерії приймання. | WHEN виконую deploy, THEN дотримуюсь послідовності Mac → GitHub → staging → prod, без правок на серверах; WHEN виконую -u/restart/deploy, THEN отримую «ок» від відповідальної особи; WHEN передаю секрети, THEN використовую heredoc згідно BP-010; WHEN виконую deploy, фінал RODO, бізнес-рішення, слова/тон, ціна/VAT, THEN залучаю людину-в-петлі. | test: перевірити, що при спробі правки на сервері процес блокується; inspect: перевірити наявність підтверджень «ок» для кожного deploy; demo: показати використання heredoc для секретів. | Must |
| PR-4 | Порушення Singular: вимога містить кілька незалежних правил (pyc, post_init_hook, -u, groups, XML-ID, No-Manual-DB, asset-only). | WHEN виконую -u, THEN очищаю .pyc; WHEN виконую оновлення існуючої БД, THEN використовую migrations/, а не post_init_hook; WHEN виконую -u, THEN перевіряю переклади з --i18n-overwrite; WHEN використовую groups= на root tree/form, THEN уникаю цього в Odoo 17; WHEN звіряю XML-ID стандартних модулів, THEN використовую ir_model_data; WHEN змінюю asset-only (scss/js/xml-шаблони), THEN бампую версію маніфеста; WHEN працюю з БД, THEN не роблю ручних змін. | test: перевірити, що після -u .pyc очищено; test: перевірити, що post_init_hook не викликається для існуючих БД; test: перевірити, що переклади перезаписуються з --i18n-overwrite; inspect: перевірити, що groups= не використовується на root tree/form; inspect: перевірити XML-ID в ir_model_data; test: перевірити, що asset-only зміни відображаються після бампу версії; inspect: перевірити відсутність ручних змін у БД. | Must |
| PR-4 | Відсутній пріоритет (MoSCoW) для кожного правила. | WHEN виконую -u, THEN очищаю .pyc; WHEN виконую оновлення існуючої БД, THEN використовую migrations/, а не post_init_hook; WHEN виконую -u, THEN перевіряю переклади з --i18n-overwrite; WHEN використовую groups= на root tree/form, THEN уникаю цього в Odoo 17; WHEN звіряю XML-ID стандартних модулів, THEN використовую ir_model_data; WHEN змінюю asset-only (scss/js/xml-шаблони), THEN бампую версію маніфеста; WHEN працюю з БД, THEN не роблю ручних змін. | test: перевірити, що після -u .pyc очищено; test: перевірити, що post_init_hook не викликається для існуючих БД; test: перевірити, що переклади перезаписуються з --i18n-overwrite; inspect: перевірити, що groups= не використовується на root tree/form; inspect: перевірити XML-ID в ir_model_data; test: перевірити, що asset-only зміни відображаються після бампу версії; inspect: перевірити відсутність ручних змін у БД. | Must |
| PR-4 | Не визначено метод верифікації для кожного правила. | WHEN виконую -u, THEN очищаю .pyc; WHEN виконую оновлення існуючої БД, THEN використовую migrations/, а не post_init_hook; WHEN виконую -u, THEN перевіряю переклади з --i18n-overwrite; WHEN використовую groups= на root tree/form, THEN уникаю цього в Odoo 17; WHEN звіряю XML-ID стандартних модулів, THEN використовую ir_model_data; WHEN змінюю asset-only (scss/js/xml-шаблони), THEN бампую версію маніфеста; WHEN працюю з БД, THEN не роблю ручних змін. | test: перевірити, що після -u .pyc очищено; test: перевірити, що post_init_hook не викликається для існуючих БД; test: перевірити, що переклади перезаписуються з --i18n-overwrite; inspect: перевірити, що groups= не використовується на root tree/form; inspect: перевірити XML-ID в ir_model_data; test: перевірити, що asset-only зміни відображаються після бампу версії; inspect: перевірити відсутність ручних змін у БД. | Must |
| PR-5 | Порушення Singular: вимога містить 8 різних критеріїв DoD. | WHEN форма відкрита: EACH з 8 DoD-критеріїв (поля, збереження, юр-документи, workflow, group-by, i18n, IA-меню, авто-розрахунки, staging-ролі, mobile-audit, ux-гейт) має окрему перевірку; IF-THEN: критерій не виконано → фіча не в DoD. | test: автоматизовані тести на кожен критерій; demo: демонстрація на staging; inspect: код-рев'ю PR. | Must |
| PR-5 | Відсутній пріоритет (MoSCoW) для кожного критерію. | WHEN визначається DoD: EACH критерій має MoSCoW-пріоритет (Must/Should/Could); IF-THEN: критерій без пріоритету → не включається в DoD. | inspect: перевірка документації DoD на наявність пріоритетів. | Should |
| PR-5 | Не визначено метод верифікації для кожного критерію. | WHEN визначається DoD: EACH критерій має метод верифікації (test/demo/inspect/analyze); IF-THEN: метод відсутній → критерій не валідний. | inspect: перевірка документації DoD на наявність методів верифікації. | Should |
| PR-6 | Відсутній пріоритет (MoSCoW) для вимоги. | WHEN створюється нова модель/фіча: MoSCoW-пріоритет Must для доказу відсутності аналога; IF-THEN: аналог є → inherit-надбудова; IF-THEN: аналога нема → власний код. | inspect: перевірка PR-опису/ADR на наявність рядка доказу. | Must |
| PR-6 | Не визначено метод верифікації для доказу 'аналога НЕМА'. | WHEN подається доказ 'аналога НЕМА': метод верифікації — inspect (читання маніфесту/моделі кандидата); IF-THEN: доказ не містить посилання на стандартний модуль → відхилити. | inspect: перевірка PR-опису/ADR на наявність конкретного модуля та результату пошуку. | Must |
| GAP-2 | Немає критерію приймання та методу верифікації. | WHEN виховник відкриває кіоск виховника, THEN він може створити до 3 звень на групу, призначити кожній дитині звенo, і діти обирають лідера зі свого звенa. IF кількість дітей у звені <5 або >10, THEN система блокує збереження. | demo: створити 3 звена, призначити 20 дітей, обрати лідерів; перевірити, що збереження блокується при 4 дітях у звені. | Must |
| GAP-2 | Не визначено пріоритет (Must/Should). | WHEN виховник відкриває кіоск виховника, THEN він може створити до 3 звень на групу, призначити кожній дитині звенo, і діти обирають лідера зі свого звенa. IF кількість дітей у звені <5 або >10, THEN система блокує збереження. | demo: створити 3 звена, призначити 20 дітей, обрати лідерів; перевірити, що збереження блокується при 4 дітях у звені. | Should |
| GAP-4 | Немає критерію приймання та методу верифікації. | WHEN запускається CI-пайплайн щотижня, THEN виконується OWASP ZAP baseline-scan на staging URL. IF знайдено вразливість з рівнем High або вище, THEN пайплайн падає і створюється issue. | analyze: перевірити наявність cron-завдання в CI та лог останнього скану; demo: запустити скан і підтвердити, що High-вразливість блокує пайплайн. | Must |
| GAP-4 | Не визначено пріоритет (Must/Should). | WHEN запускається CI-пайплайн щотижня, THEN виконується OWASP ZAP baseline-scan на staging URL. IF знайдено вразливість з рівнем High або вище, THEN пайплайн падає і створюється issue. | analyze: перевірити наявність cron-завдання в CI та лог останнього скану; demo: запустити скан і підтвердити, що High-вразливість блокує пайплайн. | Should |
| GAP-5 | Немає критерію приймання та методу верифікації. | WHEN виконується k6/locust сценарій на staging, THEN система фіксує p95 час відповіді для checkout і my. IF p95 перевищує 2 секунди при 100 одночасних користувачах, THEN тест падає і результат зберігається в CI-артефакт. | test: запустити сценарій з 100 віртуальними користувачами; analyze: перевірити, що p95 < 2с і артефакт з результатами збережено. | Must |
| GAP-5 | Не визначено пріоритет (Must/Should). | WHEN performance benchmark runs on staging for checkout and my endpoints, THEN p95 response time must be recorded and fixed in CI artifact, with target thresholds defined in N-3. Verification: demo of CI artifact containing p95 values. Priority: Must. | demo | Must |
| GAP-7 | Відсутній критерій приймання: що саме вважається виконаним (наявність документа, підписи, публікація)? | WHEN legal text for Standardy Ochrony Małoletnich is provided by owner/lawyer, THEN it must be published as data/regulamin-type document and acknowledged by staff via F-OPS-6 mechanism. Verification: inspect repository for document and acknowledgment records. Priority: Must. | inspect | Must |
| GAP-7 | Не вказано метод верифікації (inspect/test/demo) та відповідального за перевірку. | WHEN legal text for Standardy Ochrony Małoletnich is provided, THEN it must be published and acknowledged by staff via F-OPS-6 mechanism, with verification by responsible owner. Verification: inspect repository and acknowledgment logs. Priority: Must. | inspect | Must |
| GAP-7 | Відсутній пріоритет (Must/Should/Could) та дедлайн, окрім 'до cutover'. | WHEN legal text for Standardy Ochrony Małoletnich is provided, THEN it must be published and acknowledged by staff via F-OPS-6 mechanism before cutover. Verification: inspect repository and acknowledgment logs. Priority: Must. | inspect | Must |
| GAP-8 | Відсутній критерій приймання: які саме дані, через який час і за якою процедурою видаляються. | WHEN retention/erasure cron job runs, THEN it must delete specified data according to F-DOC-4 policy within defined timeframes and procedure. Verification: test cron execution and verify data deletion. Priority: Should. | test | Should |
| GAP-8 | Не вказано метод верифікації (наприклад, перевірка логів cron або тест на тестовому середовищі). | WHEN cron-запуск retention/erasure виконується, THEN система автоматично видаляє/анонімізує дані згідно з політикою [F-DOC-4]. | test | Must |
| GAP-8 | Відсутній пріоритет та посилання на конкретний механізм реалізації (cron, скрипт, інтеграція). | WHEN настає час згідно з політикою [F-DOC-4], THEN система запускає автоматичну retention/erasure-механіку через cron-завдання. | inspect | Must |
| API-2 | Вимога описує кілька контрактів (портал, кіоск, адмін) — порушення Singular; слід розбити на окремі вимоги. | WHEN користувач звертається до порталу `/my/*`, THEN система надає доступ через QWeb-сесію; WHEN користувач звертається до кіоску `/camp/kiosk/*`, THEN система обробляє запити через json-RPC з валідацією active-мов; WHEN організатор звертається до `/admin/dashboard`, THEN система надає доступ з login-as та audit-логом. | test | Must |
| API-2 | Відсутній пріоритет (Must/Should/Could) для кожного контракту. | WHEN користувач звертається до порталу `/my/*`, THEN система надає доступ через QWeb-сесію; WHEN користувач звертається до кіоску `/camp/kiosk/*`, THEN система обробляє запити через json-RPC з валідацією active-мов; WHEN організатор звертається до `/admin/dashboard`, THEN система надає доступ з login-as та audit-логом. | test | Must |
| API-3 | Відсутній пріоритет (MoSCoW) для кожної інтеграції; беклог P-3 лише для SMS-провайдерів. | WHEN система інтегрується з KSeF, TurboSMS, Zadarma або SendPulse, THEN вона використовує відповідні адаптери та документацію вендорів; WHEN зовнішній API недоступний, THEN система не блокує продаж. | inspect | Must |
| API-3 | Не вказано метод верифікації (test/demonstration/inspection/analysis) для інтеграцій. | WHEN виконується інтеграція з KSeF, TurboSMS, Zadarma або SendPulse, THEN кожен виклик зовнішнього API має бути обгорнутий у try/except, щоб збій не блокував продаж. Метод верифікації: analysis коду та документації вендорів. | analysis | Must |
| duplicates | "F-KKW-6 auto-refusal згадується в §4.4 і §8 п.7 — дубль статусу" | WHEN статус F-KKW-6 auto-refusal згадується у §4.4 та §8 п.7, THEN він має бути визначений один раз, а в іншому місці — посилання. Метод верифікації: inspection тексту. | inspection | Should |
| duplicates | "F-FIN-2 PDF-евіденція згадується в §4.11 і §8 п.12 — дубль статусу" | WHEN статус F-FIN-2 PDF-евіденція згадується у §4.11 та §8 п.12, THEN він має бути визначений один раз, а в іншому місці — посилання. Метод верифікації: inspection тексту. | inspection | Should |
| duplicates | "F-KSK-2 R4b згадується в §4.7 і §8 п.1 — дубль статусу" | WHEN статус F-KSK-2 R4b згадується у §4.7 та §8 п.1, THEN він має бути визначений один раз, а в іншому місці — посилання. Метод верифікації: inspection тексту. | inspection | Should |
| duplicates | "F-RODO-2 attachment-ACL згадується в §4.5 і §8 п.8 — дубль статусу" | WHEN статус F-RODO-2 attachment-ACL згадується у §4.5 та §8 п.8, THEN він має бути визначений один раз, а в іншому місці — посилання. Метод верифікації: inspection тексту. | inspection | Should |
| duplicates | "F-FIN-3 BEP-розрив згадується в §4.11 і §8 п.5 — дубль статусу" | WHEN F-FIN-3 BEP-розрив згадується у §4.11 ТА §8 п.5, THEN статус має бути єдиним джерелом істини (не дублюватися), AND кожна згадка має посилатися на первинне визначення. | inspect: перевірити, що у §4.11 і §8 п.5 немає окремих статусів, лише посилання на первинне визначення. | Must |
| duplicates | "F-KAM-2 п.9 opis згадується в §4.6 і §8 п.6 — дубль статусу" | WHEN F-KAM-2 п.9 opis згадується у §4.6 ТА §8 п.6, THEN статус має бути єдиним джерелом істини (не дублюватися), AND кожна згадка має посилатися на первинне визначення. | inspect: перевірити, що у §4.6 і §8 п.6 немає окремих статусів, лише посилання на первинне визначення. | Must |
| conflicts | "F-REC-1 (власна модель рекрутації) vs PR-6 (антиприклад — власна рекрутація) — конфлікт політики" | WHEN F-REC-1 визначає власну модель рекрутації, THEN вона має бути узгоджена з PR-6 (антиприклад), IF-THEN конфлікт політики усунуто шляхом явного розмежування сценаріїв застосування. | analyze: порівняти тексти F-REC-1 та PR-6, перевірити наявність явного розмежування сценаріїв. | Must |
| conflicts | "F-REC-1a (стопгап 07.07) vs F-REC-1 (основна модель) — конфлікт архітектури" | WHEN F-REC-1a (стопгап 07.07) активний, THEN він має бути архітектурно узгоджений з F-REC-1 (основна модель), IF-THEN конфлікт архітектури усунуто шляхом визначення пріоритету стопгапу. | analyze: перевірити, що F-REC-1a та F-REC-1 мають узгоджену архітектуру та визначений пріоритет. | Must |
| conflicts | "F-KKW-6 (refund заглушка) vs F-FIN-5 (повне повернення коштів) — конфлікт реалізації" | WHEN F-KKW-6 реалізує refund заглушку, THEN вона має бути сумісна з F-FIN-5 (повне повернення коштів), IF-THEN конфлікт реалізації усунуто шляхом визначення умов активації повного повернення. | test: перевірити, що при активації F-FIN-5 заглушка F-KKW-6 не блокує повне повернення коштів. | Must |
| conflicts | "F-VAT-2 (VAT zw. default) vs F-VAT-4 (VAT-marża на фактурі) — конфлікт режиму" | WHEN фактура виставляється в режимі VAT-marża (F-VAT-4), THEN система НЕ дозволяє встановити VAT zw. (F-VAT-2) на тому ж документі; конфлікт режимів має блокуватись. | test: створити фактуру з VAT-marża та спробувати змінити на VAT zw.; очікується помилка валідації. | Must |
| conflicts | "F-I18N-3 (деплой з --i18n-overwrite) vs PR-4 (asset-only зміни вимагають бампу версії) — конфлікт процесу" | WHEN деплой виконується з --i18n-overwrite (F-I18N-3) І зміни стосуються лише asset-файлів, THEN версія ПЗ має бути збільшена (PR-4); інакше деплой відхиляється. | test: запустити деплой з --i18n-overwrite без бампу версії; очікується відмова. | Must |
| conflicts | "F-RODO-7 (шифрування at-rest) vs M-4 (прод живий без нейтралізації) — конфлікт безпеки" | WHILE прод-середовище працює без нейтралізації інциденту (M-4), THEN шифрування даних at-rest (F-RODO-7) має бути активоване; інакше система вважається невідповідною. | inspect: перевірити конфігурацію шифрування на проді; demo: показати шифрування. | Must |
| conflicts | "F-KSK-2 (R4b виправлено 04.07) vs §8 п.1 (R4+R4b у черзі) — конфлікт статусу" | WHEN R4b виправлено 04.07 (F-KSK-2), THEN у черзі виправлень (згідно §8 п.1) R4 та R4b мають бути позначені як виконані; інакше статус конфліктує. | inspect: перевірити статус R4 та R4b у черзі. | Must |
| conflicts | "F-FIN-2 (PDF є) vs §8 п.12 (залишок XLSX) — конфлікт статусу" | WHEN PDF-файл існує (F-FIN-2), THEN залишок у форматі XLSX (згідно §8 п.12) має бути відсутній або позначений як застарілий; інакше статус конфліктує. | inspect: перевірити наявність XLSX-файлу та його статус. | Must |
| conflicts | "F-KAM-2 (п.9 закрито) vs §8 п.6 (у черзі) — конфлікт статусу" | WHEN статус F-KAM-2 = 'закрито' ТА §8 п.6 = 'у черзі', ТОДІ система має узгодити статуси: F-KAM-2 не може бути закритим, якщо §8 п.6 не виконано. | inspect: перевірити статуси F-KAM-2 та §8 п.6 у реєстрі вимог; конфлікт відсутній, якщо обидва статуси узгоджені. | Must |
| conflicts | "F-RODO-2 (HTTP-ізоляція вирішена) vs §8 п.8 (attachment-ACL у черзі) — конфлікт статусу" | WHEN F-RODO-2 = 'вирішено' ТА §8 п.8 = 'у черзі', ТОДІ система має узгодити статуси: F-RODO-2 не може бути вирішеним, якщо attachment-ACL не виконано. | inspect: перевірити статуси F-RODO-2 та §8 п.8 у реєстрі вимог; конфлікт відсутній, якщо обидва статуси узгоджені. | Must |
| gaps | "F-WIZ-8: фото-крок у wizard не верифіковано — дія без критерію приймання" | WHEN користувач проходить wizard, ТОДІ фото-крок має бути верифікований: наявність фото обов'язкова, інакше крок не завершується. | test: виконати wizard без фото; крок не завершується, якщо фото відсутнє. | Should |
| gaps | "F-PRG-3: правило 100% заповнення не підтверджено — дія без критерію" | WHEN заповнюється форма, ТОДІ правило 100% заповнення має бути підтверджено: всі обов'язкові поля заповнені, інакше форма не приймається. | test: спроба відправки форми з пропущеним полем; форма відхиляється. | Should |
| gaps | "F-REC-4: ставки на вакансії не верифіковано — дія без критерію" | WHEN вакансія має ставки, ТОДІ ставки мають бути верифіковані: значення ставок відповідають заданим критеріям, інакше вакансія не публікується. | test: введення некоректних ставок; вакансія не публікується. | Should |
| gaps | "F-KKW-4: constraint на активності не верифіковано — дія без критерію" | WHEN активність виконується, THEN система застосовує constraint F-KKW-4 та блокує дію, IF порушено обмеження | test | Must |
| gaps | "F-KKW-3: хеш PDF не підтверджено — дія без критерію" | WHEN PDF завантажується, THEN система обчислює та зберігає хеш файлу, IF хеш не збігається — відхиляє | test | Must |
| gaps | "F-KAM-3: SLA-таймери не верифіковано — дія без критерію" | WHEN SLA-таймер запускається, THEN система фіксує час старту та контролює дедлайн, IF перевищено поріг — сповіщає | test | Should |
| gaps | "F-MY-3: блок виховника не верифіковано — дія без критерію" | WHEN блок виховника відображається, THEN система показує дані з джерела, IF дані відсутні — показує порожній стан | demo | Should |
| gaps | "F-MY-4: погода-день не верифіковано — дія без критерію" | WHEN відображається погода на день, THEN система показує прогноз з API, IF дані недоступні — показує заглушку | demo | Could |
| gaps | "F-MY-5: сторінка preferences не знайдена — дія без критерію" | WHEN користувач відкриває сторінку preferences, THEN вона має бути доступною за URL /preferences, інакше система показує помилку 404. | test: перехід на /preferences повертає HTTP 200 | Must |
| gaps | "F-COM-3: матриця auto-subscribe не верифікована — дія без критерію" | WHEN користувач змінює налаштування auto-subscribe, THEN матриця підписок оновлюється відповідно до правил, інакше зміни не зберігаються. | test: зміна прапорця auto-subscribe оновлює стан підписки | Should |
| gaps | "F-SALE-2: відповідність канону Offer 2026 не доведена — дія без критерію" | WHEN пропозиція створюється, THEN вона відповідає канону Offer 2026, інакше вона відхиляється. | inspect: перевірка відповідності полів канону Offer 2026 | Must |
| gaps | "F-FIN-1: аналітика-автостворення не верифіковано — дія без критерію" | WHEN аналітика створюється автоматично, THEN вона генерується без ручного втручання, інакше процес зупиняється. | demo: запуск автостворення аналітики без ручних дій | Should |
| gaps | "F-OPS-1: агрегат для кухні не доведено — дія без критерію" | WHEN агрегат для кухні використовується, THEN він має бути підтверджений робочими даними, інакше він неактивний. | analyze: перевірка наявності підтверджуючих даних агрегату | Could |
| gaps | "F-OPS-4: /my/training не збудовано — дія без критерію" | WHEN /my/training is requested THEN system MUST return HTTP 200 with non-empty HTML body; verification: test. | test | Must |
| gaps | "F-DOC-2: ZIP-збірка не верифікована — дія без критерію" | WHEN ZIP build is triggered THEN system MUST produce a valid ZIP archive containing all expected files; verification: test. | test | Must |
| gaps | "F-VAT-3: ланцюг форма→колонтитули не доведено — дія без критерію" | WHEN form is submitted THEN system MUST render headers/footers consistently across all pages; verification: test. | test | Should |

## Джерела (цитувати у звіті)
- ISO/IEC/IEEE 29148:2018 — Systems and software engineering — Requirements engineering. Шаблон/огляд: https://www.well-architected-guide.com/documents/iso-iec-ieee-29148-template/ ; https://www.cwnp.com/req-eng/
- EARS (Easy Approach to Requirements Syntax), Mavin et al. — шаблони WHEN/WHILE/IF-THEN/WHERE.
- Spec-Driven Development: From Code to Contract in the Age of AI Coding Assistants — https://arxiv.org/html/2602.00180v1
- Spec-Driven Development: The Definitive 2026 Guide — https://thebcms.com/blog/spec-driven-development
- How To Write Clear, Measurable Software Requirements in 2026 — https://www.designrush.com/agency/software-development/trends/software-requirements-specification
- Requirements Smells (Femmer et al.) — https://arxiv.org/pdf/1611.08847
- Well-Formed Quality of System Requirements for ISO 29148-2018 (NLP metric) — https://www.researchgate.net/publication/385802396
- QUARE: Quality-Aware Requirements Analysis through Multi-Agent Dialectical Negotiation — https://arxiv.org/pdf/2603.11890
